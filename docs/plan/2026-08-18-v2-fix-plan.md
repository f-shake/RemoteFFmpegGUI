# v2 修复计划

- **制定时间**：2026-08-18
- **依据**：[../code-review/2026-08-18-v2-review.md](../code-review/2026-08-18-v2-review.md)
- **决策**：v2 阶段**保留 WPF**（Avalonia 重构搁置）；本计划覆盖 WPF 相关问题的修复
- **原则**：按批次执行，前一批次是后一批次的前提；每项含验证方式，完成后更新审查报告为已修复

---

## 批次 0：阻断项（不修则编译/构建/发布/测试全断）

### P0-1 FzLib 双轨方案：WPF 内化源码 + 其余项目 NuGet（阻断编译）⭐
- **关联**：审查 §二.8 实测验证、§三.29；决策 Q5/Q10-Q14（2026-08-18 确认）
- **现状**：Core/WebAPI/WebTest Debug 引用 `$(LocalFzLibPath)`（OneDrive 个人路径，不存在）；Release 走 NuGet FzLib 3.3.2（**缺 `FzLib.Web` 命名空间**，编译不过）；WPF 引用 `libs/FzLib.dll`（3.4.0-rc.2，冗余——WPF 实际零使用）+ `libs/FzStandardLib.dll` + `libs/FzCoreLib.Windows.dll`（老版，WPF 实际依赖）
- **方案（已确认，双轨）**：
  - **WPF 轨：内化老版 FzLib 源码**
    1. 从 `temp/FzLib` 的 **v1_final 标签**取源文件（已核实全部存在于 v1_final）：FzStandardLib 部分 4 个（`Program/App.cs`、`INotifyPropertyChangedExtension.cs`、`Program/Runtime/UnhandledExceptionCatcher.cs`、`Collection/DictionaryExtension.cs`）+ FzCoreLib.Windows 部分 17 个（`Program/Runtime/UnhandledException.cs`+`TrayIcon.cs`、`WPF/FrameworkElementExtension.cs`、`WindowExtension.cs`、`DependencyObjectUtillity.cs`、`Controls/SmoothScrollViewerHelper.cs`、9 个转换器 + `IDefaultNullValue.cs` + `Converters.xaml`）
    2. **不保留 `FzLib.*` 命名空间**：源码放入 WPF 项目内（如 `SimpleFFmpegGUI.WPF/FzLib/`），**命名空间与目录结构改为项目自己的**（如 `SimpleFFmpegGUI.WPF.FzLib.*`），所有 using/XAML xmlns 同步改写
    3. `Converters.xaml` **裁剪为实际使用的 9 个转换器**（Bool2Visibility、Equal、InverseBool、NotNull、Description、Bool2String、CountMoreThanZero、StringReplace、TimeSpan），`App.xaml:14` 的 pack URI 改为本地相对路径
    4. WPF csproj：删除 4 条 `Reference`（FzLib.dll×2、FzStandardLib、FzCoreLib.Windows），加 `<UseWindowsForms>true</UseWindowsForms>`（TrayIcon 的 NotifyIcon、SmoothScrollViewerHelper 的 SystemInformation）
    5. 清理死 using（`Config.cs` 的 `FzLib.DataStorage.Serialization` 等）
  - **其余项目轨：NuGet FzLib 3.4.0-rc.2**（NuGet 已发布，含 `FzLib.Web`）
    1. `Directory.Packages.props`：FzLib 3.3.2 → **3.4.0-rc.2**
    2. 删除 `Directory.Build.props` 的 OneDrive 路径机制（`UseLocalFzLib`/`LocalFzLibPath`）与注释死块，删除 Core/WebAPI/WebTest csproj 的条件 Reference
  - **libs 清理**：删除 `FzLib.dll`、`FzStandardLib.dll`、`FzCoreLib.Windows.dll`、`ModernWpf.FzExtension.dll`、`WindowsAPICodePack.FzExtension.dll`；**保留** `Enterwell.Clients.Wpf.Notifications.dll`（WPF 仍引用，第三方）
- **命名空间冲突核查（已做）**：老版类型（`FzLib.Program.*`/`FzLib.Collection.*`/`FzLib.WPF.*`）与 3.4.0-rc.2（`FzLib.Application/Collections/Programming/Web/IO/Numeric/Text`）**无同名类型**；内化改项目命名空间后彻底隔离
- **验证**：默认 `dotnet build -c Debug` 与 `-c Release` 均 0 错误（无 OneDrive 依赖）；WPF 启动运行正常（TrayIcon、异常捕获、平滑滚动、转换器）；发布包无 FzLib 相关 DLL（除 NuGet 带入的统一版本）
- **工作量**：M（内化 21 文件 + XAML 改写，机械性为主）

### P0-2 前端 `npm run build` 修复（阻断构建）
- **关联**：审查 §四 低
- **方案**：删除 `Web/tsconfig.app.json:6` 的 `"baseUrl": "."`（Vite 下 `paths` 无需 baseUrl）；如保留则加 `"ignoreDeprecations": "6.0"`
- **验证**：`npm run build` 通过（vue-tsc + vite）
- **工作量**：S

### P0-3 build.ps1 / clean.ps1 适配 v2（阻断发布）
- **关联**：审查 §二.8、§三 M2
- **方案**：
  - 删除对 Host/Host.Console/Host.WindowsService 的 3 个 `dotnet publish`（build.ps1:70-72）
  - 增加 `Copy-Item SimpleFFmpegGUI.WebAPI/CreateWindowsService.bat` 到发布包（README 部署章节需要它）
  - 删除未使用的 `-s`/`-f` 参数（build.ps1:5-6）
  - 修正 `Remove-Item Generation/Release`（build.ps1:101）：Inkore.Extension 输出在 `..\Release\AnyCPU`（其 csproj 覆盖了统一输出路径），误删该行或改为只清空已生成的 WebPackage 子目录
  - clean.ps1 改为清理 `Generation/bin`、`Generation/obj`（Directory.Build.props 重定向后的实际位置），或改用 `dotnet clean`
- **验证**：`./build.ps1 -w` 生成完整 WebPackage；clean.ps1 实际清空
- **工作量**：S

### P0-4 WebTest 测试基建修复（阻断测试）
- **关联**：审查 §二.6
- **方案**：
  1. `SimpleFFmpegWebApplicationFactory`：测试前把仓库 `bin/ffmpeg/*` 复制到测试输出目录（或配置 FFmpegDir 指向仓库 bin），并处理 `HostAbortedException` 导致 `EnsureCreated` 不执行的问题——在 fixture 里显式调用 `MigrateIfNeeded` + `EnsureCreated`（或让 `AppLifetimeService` 容错）
  2. 测试视频：`appsettings.test.json` 增加 `TestVideo10s`/`TestOutputVideo10s` 配置键（AppTestSettingsKeys.cs 已定义但被注释、AppTestSettings 属性已存在）；准备一个 10 秒小视频提交仓库（`WebTest/test/`），并在 fixture 中复制到 InputDir
  3. `AppTestSettingsKeys.cs` 取消注释并接入 `Configure<AppTestSettings>`（SimpleFFmpegWebApplicationFactory 需确认已绑定）
- **验证**：`dotnet test` 全绿（或仅剩明确标记的环境用例）
- **工作量**：M

---

## 批次 1：功能损坏（用户可感知的错误行为）

### P1-1 合并音视频任务类型修复 ⭐
- **关联**：审查 §二.1
- **方案**：`Core/Services/TaskService.cs:63` 改为 `type.ToLower() == "mux" ? TaskType.Mux : TaskType.QualityCheck`（或直接按 switch 分支给值，不再用字符串判断）
- **验证**：WebTest 新增 Mux 用例（见 P4-3）断言任务类型
- **工作量**：S

### P1-2 队列 cancelQueue 永久置位修复 ⭐
- **关联**：审查 §二.2
- **方案**：`Core/Services/QueueService.cs:135-159` 用 try/finally 包裹循环，`cancelQueue = false` 移到 finally；循环内异常应记日志（Logger.Error）而非静默；同时修复任务被独立任务抢走导致的 NRE（ProcessTaskAsync 前校验/加锁）与 DB 保存失败时 `RemoveManager` 泄漏（finally 中调用）
- **验证**：WebTest 或手工：制造一次异常后再次 StartQueue 仍能执行
- **工作量**：M

### P1-3 默认进程优先级接线 ⭐
- **关联**：审查 §二.3
- **方案**：二选一：
  - ConfigController/ConfigService 写入后，FFmpegProcessService 启动进程时从 ConfigService 读取（需把 ConfigService 注入 FFmpegProcessService 或经 AppSettings 桥接）
  - 或 ConfigController 直接写 `IOptions<AppSettings>.Value.DefaultProcessPriority`（内存生效）并落盘 config.json
  - `ConfigController.SetDefaultProcessPriority` 需调用 `SaveAsync`
- **验证**：Web 设置优先级 → 新建任务 → 实际进程优先级生效（TaskManager 可见）
- **工作量**：M

### P1-4 预设"编辑/更新"前端接线 ⭐
- **关联**：审查 §三.13
- **方案**：`Web/api.ts` 增加 `updatePreset(id, params)`（`POST Preset/{id}`，后端已有 `PresetController.UpdateAsync`）；`Presets.vue` 编辑对话框区分"新建/更新"（按是否有 id 路由到不同接口），补名称输入框；`CodeArguments.vue` 的"更新"按钮同样接线
- **验证**：编辑已有预设保存不再 409，参数生效
- **工作量**：S

### P1-5 未登录 `Bearer undefined` 修复
- **关联**：审查 §三.14
- **方案**：`Web/App.vue:188` 或 `api.ts:setHeader()` 改为 token 存在时才设置 Authorization 头（cookie 缺失时删除 header 而非设 `Bearer undefined`）
- **验证**：未登录访问页面无"Token不正确"错误通知
- **工作量**：S

### P1-6 `processedOptions` 字段名对齐
- **关联**：审查 §三.15
- **方案**：`CodeArguments.vue:371`、`CodeArgumentsDescription.vue:98,100` 的 `args.processedOptions` 改为 `args.processedOperationParameters`
- **验证**：保存预设含"删除输入文件"→ 重载预设设置保留；任务详情两行显示正确
- **工作量**：S

### P1-7 Mux"长度处理"死控件修复
- **关联**：审查 §三.16
- **方案**：前端 `getArgs()` 序列化 `mux.shortest`（对齐后端 `MuxParameters.Shortest` 的 JSON 键）；后端 `FFmpegTaskService.RunCombineProcessAsync` 读取并应用 `Shortest`（当前只有旧数据转换在用）
- **验证**：Web 切换"裁剪到最短"后任务实际输出长度正确
- **工作量**：M（需确认 v2 后端的合并处理逻辑完整性）

### P1-8 默认输出路径落到输入目录
- **关联**：审查 §三.19
- **方案**：`Core/Services/TaskService.cs:191-218`（GetOutputByInput）：对已绝对化的输入路径，改用 `Path.Combine(OutputDir, Path.GetFileName(input))`（恢复 v1 行为），或 `FilePathHelper` 增加"绝对路径仍拼进 rootDir"的方法
- **验证**：不指定输出时，任务输出在 OutputDir 下而非输入旁
- **工作量**：S

### P1-9 预设导入 Type=3 修复
- **关联**：审查 §三.20
- **方案**：`Core/Compatibility/PresetConverter.cs:42-51` 增加 `old.Type == 3 → TaskType.Custom(99)` 映射（与 DatabaseMigrator 一致）；补 WebTest 导入用例
- **验证**：v1 导出的 Custom 预设 JSON 导入后可查、可建任务
- **工作量**：S

### P1-10 MediaInfoService 预设推断 KeyNotFoundException
- **关联**：审查 §三.21
- **方案**：`Core/Services/MediaInfoService.cs:47-146` 对 `settings[...]` 全部改用 `TryGetValue` 或提前 `ContainsKey` 校验（x264/x265 缺键时跳过该字段）
- **验证**：对缺少 rc/subme/ref 的源文件调用"读取编码设置"不再 500
- **工作量**：S

### P1-11 数据库迁移保留用户配置（Configs 表不 DROP）⭐
- **关联**：审查 §三.25；决策 Q7（2026-08-18 确认修复）
- **方案**：`Core/Compatibility/DatabaseMigrator.cs:195-200`：迁移时读出 v1 Configs 表的用户设置（DefaultProcessPriority、SnapshotSize 等），写入 v2 配置存储（config.json 或新表），替代直接 DROP
- **验证**：带 v1 数据库升级后设置保留（配合 P1-12）
- **工作量**：M

### P1-12 SnapshotSize 恢复为配置项
- **关联**：审查 §三.25；决策 Q8（2026-08-18 确认恢复）
- **方案**：`Core/Services/MediaInfoService.cs:181` 硬编码 "-1:1080" 改为读取配置（AppSettings 或 ConfigService 键），Web 与 WPF 提供设置入口（v1 语义）；与 P1-11 联动，迁移时带上旧值
- **验证**：改配置后截图分辨率生效
- **工作量**：S

---

## 批次 2：WPF 修复（v2 保留 WPF）

### P2-1 WPF 启动托管服务 ⭐
- **关联**：审查 §二.4
- **方案**：`WPF/App.xaml.cs:84-88`：BuildServiceProvider 后手动启动 `DbLoggerService` 与 `AppLifetimeService`（取实例调 `StartAsync(CancellationToken.None)`），退出时 `StopAsync`；或更彻底——WPF 用 `Host.CreateApplicationBuilder` 包一层（改动大，不推荐先行）
- **验证**：WPF 运行中 LogsPage 能实时看到本会话日志；崩溃后重启，遗留 Processing 任务被复位为 Error
- **工作量**：S

### P2-2 WPF 远程主机协议适配 ⭐
- **关联**：审查 §二.5
- **方案**：`WPF/ViewModels/AddTaskPageViewModel.cs:58-84, 222-288`：
  1. 认证头加 `Bearer ` 前缀（:69）
  2. 请求体属性 `Argument` → `Parameter`（:260-265）
  3. 输入路径 `":"` 前缀约定：改为直接传相对文件名（v2 `FilePathHelper` 按 InputDir 相对解析）；或后端 `FilePathHelper.GetFullPath` 增加对 `":"` 前缀的兼容处理（推荐前者，前端改）
- **验证**：WPF 配置远程主机（v2 WebAPI）后能成功推送任务并启动队列
- **工作量**：M

### P2-3 QueueService Managers 并发同步
- **关联**：审查 §三.22
- **方案**：`Core/Services/QueueService.cs` 的 `taskProcessManagers` 改为并发集合（`ConcurrentDictionary<int, FFmpegTaskService>` 或加锁）；`Managers`/`Tasks` 返回快照；`TaskManagersChanged` 事件在 UI 线程编组（WPF 侧）或文档化线程要求
- **验证**：并发独立任务 + 浏览列表无 InvalidOperationException
- **工作量**：M

### P2-4 SyncModifiedTime NRE（WPF/Web 共用）
- **关联**：审查 §三.17
- **方案**：`Core/Services/FFmpegTaskService.cs:240-245` 增加 `task.Inputs.Count > 0` 守卫（对齐 254 行写法）
- **验证**：Custom 任务勾选"同步修改时间"不再报错
- **工作量**：S

### P2-5 取消/暂停"准备中"窗口 NRE（WPF/Web 共用）
- **关联**：审查 §三.18
- **方案**：`FFmpegTaskService.cs:148-153, 158-171` 的 `cancel`/`Process` 增加 null 防御（`?.Cancel()`、`Process?.WaitForExitAsync()`）；取消在准备窗口内时置标志，RunAsync 启动前检查
- **验证**：创建任务后立刻取消，不 500、状态最终为 Cancel
- **工作量**：M

### P2-6 CurrentTasksViewModel 行为（已决策：保持 v2 现状）
- **关联**：审查 §四 行为变化；决策 Q15（2026-08-18 确认）
- **问题**：v2 显示全部历史 Processing+Queue 任务；v1 只显示本次启动后的
- **决策**：保持现状（队列持久化是 v2 特性，列表显示全部未完成任务是配套行为）；**无代码改动**，仅在文档中说明行为
- **工作量**：-

---

## 批次 3：安全与健壮性

### P3-1 路径穿越与任意路径读写收口 ⭐
- **关联**：审查 §二.9；决策 Q16（2026-08-18 确认：收口）
- **方案**：`Core/Helpers/FilePathHelper.cs:25-33`：相对路径先规范化（`Path.GetFullPath`）再校验结果 `StartsWith(rootDir)`，拒绝 `..` 逃逸；**绝对路径一律拒绝**（v1 输入路径本就不接受任意绝对路径，Download 的绝对路径能力只是 Path.Combine 副作用，一并关掉）；`FileController.Download`/`UploadFile`（FileName 含 `..\` 拒绝）、`MediaInfoController.GetAsync` 复用该校验；同步删除/修改 WebTest 中绝对路径下载用例（FileApiTests:25-27）
- **验证**：`../` 请求返回 4xx；绝对路径请求返回 4xx；上传文件名含 `..` 被拒
- **工作量**：M

### P3-2 出厂默认 Token 与作者路径
- **关联**：审查 §二.10
- **方案**：`Core/appsettings.json`：`Token` 出厂值清空（空 = 不鉴权，与 AppActionFilter 逻辑一致）或改为部署时必改的占位并加注释；`InputDir/OutputDir` 改为相对/通用示例路径。**注意**：WebTest 依赖 Token 配置，改后需同步测试工厂（测试用自己的 appsettings.test 覆盖）
- **验证**：部署包默认无公开口令；README 部署章节强调必改项
- **工作量**：S

### P3-3 AppActionFilter 异常处理修正 ⭐
- **关联**：审查 §二.7
- **方案**：`WebAPI/AppActionFilter.cs:32-40`：Message 为空时也设置 `Result = StatusCodeResult(500)`；生产环境不把异常 Message 直接回客户端（返回通用 500 文案，详情写日志）；HttpStatusCodeException 保持
- **验证**：模拟空 Message 异常返回 500 而非 200；响应不含 SQL/路径
- **工作量**：S

### P3-4 FTP 匿名认证（已决策：保持匿名 + README 说明）
- **关联**：审查 §三.26；决策 Q17（2026-08-18 确认）
- **方案**：保持匿名（FTP 默认关闭、手动开关，部署在可信环境）；README 明示风险 + 引导修改端口；**无代码改动**（仅文档）
- **工作量**：S（文档）

### P3-5 空引用与参数校验批量修复
- **关联**：审查 §四 健壮性
- **项**：`PresetController.ImportAsync`/`FileController.UploadFile`（file null）、批量任务 body null、`ConfigController` 优先级范围 0-5 校验、`MediaInfoController.GetSnapshotAsync` seconds 非负、`TaskService.cs:111` ids 重复去重、`FtpService` 重复启动/未启动关闭改友好异常
- **验证**：对应 WebTest 补用例
- **工作量**：M

### P3-6 快照临时文件清理
- **关联**：审查 §四
- **方案**：`MediaInfoService.cs:211-221` 返回临时文件路径改为用后即删（控制器 `PhysicalFile` 后删除，或改为内存流/缓存 + 定期清理）
- **工作量**：S

### P3-7 生产环境全局异常处理 + 日志管线
- **关联**：审查 §三.12、§三.28、§三.11
- **方案**：`Program.cs`：加 `UseExceptionHandler`（非开发环境）；`builder.Logging.AddSerilog()` 接入 host 管线；调整启动顺序让 MigrateDb 异常至少落文件日志（把 Serilog 初始化提前）
- **验证**：生产模式控制器外异常返回 500 且写 `logs/logs.txt`
- **工作量**：M

---

## 批次 4：测试完善

### P4-1 测试稳定性
- **关联**：审查 §三.23
- **方案**：`TaskAndQueueApiTests` 去掉固定 sleep：轮询任务状态（带超时）代替；`GetProcessCount` 改为断言任务状态而非机器级进程数（或限定只统计测试进程的 ffmpeg 子进程）
- **验证**：连续跑 3 次全绿
- **工作量**：M

### P4-2 CompatibilityMigrationTests 真实覆盖
- **关联**：审查 §三.24
- **方案**：提交一个 v1.1 版 `old_db.sqlite` 样本到 `WebTest/test/`（或测试内动态构造 v1 schema + 数据），移除"找不到即跳过"的假通过逻辑
- **验证**：迁移测试真实执行断言
- **工作量**：M

### P4-3 缺失用例补充
- **关联**：审查 §六"未覆盖的功能"
- **项**：Mux/Concat/QualityCheck/Custom 任务创建用例（含 P1-1 回归）、无 token 401 反向用例、Log API 用例、FTP 开关边界用例
- **验证**：`dotnet test` 全绿且覆盖上述场景
- **工作量**：M

### P4-4 拼写错误与断言修正
- **关联**：审查 §四 测试细节
- **方案**：`PresetController.cs:89` `presetsService.json` → `presets.json`，同步改 `PresetApiTests.cs:57` 断言
- **工作量**：S

---

## 批次 5：清理与文档

### P5-1 README 全面修正（含 WPF 保留决策）
- **关联**：审查 §二.8、§三.27、§四
- **项**：架构表（删 Host 系列、加 WPF/WebTest）、部署章节（删"启动 Host"、改 WebAPI 直接部署 + CreateWindowsService.bat）、删 `InputDirAccessable` 文档、`-w` 说明、v2 计划章节（Avalonia 项改为"已决定保留 WPF"）、vite 代理描述（改为 CORS + 直连 localhost:5001）、WPF 保留说明
- **工作量**：S

### P5-2 死配置与死代码清理
- **关联**：审查 §四
- **项**：`Directory.Build.props` 注释死块与 `UseLocalFzLib` 残留（随 P0-1 一并）、`Directory.Packages.props` 残留（Furion/JKang.IpcServiceFramework/log4net/CommandLineParser 等，核对无引用后删除）、`FFmpegDbContext` 无参构造/`Check()`/注释 Migrate 段、`AppSettings.LocalSqlite` 绑定、FFmpegLib 不可达 throw 与 `MaxSpeedLevel=10` 越界（`FFmpegEnums.Presets` 补到 10 或 MaxSpeedLevel 改 9）、`N_VideoCodec.SpeedFPSRelationship`、`/password` 路由、`public/index.html`、console.log、`process.env.NODE_ENV`
- **验证**：编译 0 警告回归对比
- **工作量**：M

### P5-3 杂项
- **关联**：审查 §四
- **项**：`libs/ModernWpf.FzExtension.dll`、`WindowsAPICodePack.FzExtension.dll` 孤儿 DLL 删除；`LogsPageViewModel.cs` 重复 using；`App.xaml.cs` 双 ServiceProvider 简化；`FtpService` 终结器 SuppressFinalize；`日志.md` 补记 v2 开发记录；`Web/README.md` Vite 模板文案替换
- **工作量**：S

---

## 执行顺序建议

```
批次 0（1 天）→ 批次 1（2-3 天）→ 批次 2（1-2 天）
→ 批次 3（1-2 天）→ 批次 4（1 天）→ 批次 5（半天）
```

- **P0-1 / P0-2 / P0-3 做完即可联调**（编译 + 构建 + 发布通了再修功能）
- 批次 1 中 ⭐ 四项（P1-1/P1-2/P1-3/P1-4）优先，都是用户直接感知的功能错误
- P1-8 输出路径、P1-5 401 弹错、P1-6 设置丢失属于"静默错误"，修完记得回归 v1 行为
- **决策清单（2026-08-18 全部确认）**：保留 WPF（Avalonia 搁置）；FzLib 双轨（WPF 内化 v1_final 源码改项目命名空间 + 其余项目 NuGet 3.4.0-rc.2）；迁移保留 Configs 配置；SnapshotSize 恢复配置；InputDirAccessable 不恢复（部署文档约束）；任务列表保持 v2 现状；绝对路径访问收口；FTP 保持匿名（文档说明）；测试范围 = 修复项 + 缺失用例补齐；直接在 master_v2 修改；每批完成后三关验证（编译+测试+前端构建）

---

*生成：Claude Code。与 [../code-review/2026-08-18-v2-review.md](../code-review/2026-08-18-v2-review.md) 对应，修复完成一项请在该报告中标注。*
