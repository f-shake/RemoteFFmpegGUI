namespace SimpleFFmpegGUI;

/// <summary>
/// 应用程序元信息。
/// </summary>
public static class AppInfo
{
    /// <summary>
    /// 应用程序语义版本。数据库 schema 版本跟随它（数据库版本 = 该值）。
    /// 发布新版本（含补丁/小版本，如 2.0.1、2.1.0）时手工同步。
    /// </summary>
    public const string AppVersion = "2.0.1";
}
