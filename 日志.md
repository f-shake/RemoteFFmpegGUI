# FFmpeg工具箱日志

## 20210805

做了一番比较以后，最终确定如下结构，并做了初步测试：


|项目名|项目名（中文）|介绍|
|-|-|-|
|Core|核心|提供Host和WPF、Web的契约以及公共方法|
|Host|主机|对FFmpeg进行包装，实现其功能，并通过NamedPipe进行发布|
|WPF|桌面GUI|本地执行的桌面端的GUI实现|
|WebAPI|服务端|使用ASP.NetCore实现的服务器|
|Web|Web客户端|使用Vue.js实现的网页端|

完成了Host与WebAPI之间的连接、WebAPI和Web之间的连接

## 20210810

搭建中。。初步完成了新增转码任务

## 20210811

【Core】新增了数据库连接

【Core】初步完成了任务队列系统

【Web】新增任务列表界面

## 20210812

【Web】进一步完成了任务的删除等、完成新增编码任务的界面等

【Web】优化界面，初步完成编码预设的后端

## 20210813

【Core】【Web】新增多文件拼接的支持

【Core】【Web】新增视频裁剪的支持

【Web】支持了新增、更新、调用预设

【Web】【Web】支持了额外参数

## 20210814

【Core】将Host的众多类移到Core中，Host仅用于提供管道通信

【Core】将DbContext改为单例模式

【Web】支持自定义管道名

【Web】新增FTP服务器功能

## 20210815

【Web】修改管道客户端类为依赖注入

【Web】新增在调用管道时，若检测到提供商未运行，则会自动启动

【Web】完成对任务列表的分页和筛选

【Web】修复了获取数据库信息时，并行查询报错的BUG

【Web】新增预设列表和日志列表

【Web】优化了错误信息的提示

## 20210816

【Web】新增支持上传小文件和直接下载输出文件

【Web】优化：在滚动时，左侧导航栏将固定

【Web】修复了当IIS无权限访问输入输出文件夹时无法使用的问题，通过Host访问来解决

【Web】优化了UI

## 20210817

【Web】优化了UI

【Web】新增支持视频裁剪设置中，可以粘贴一串时间字符串后自动解析为时分秒

【Core】【Web】新增支持不导出视频或不导出音频

## 20210818

【Web】优化了UI

【Core】【Web】支持指定视频容器

【Core】【Web】新增合并音视频功能

【Web】基本完成token认证功能

## 20210819

【Core】【Web】新增视频对比功能

【Web】新增重制时，可以保留文件

【Core】【Web】新增WebM的VP9和OPUS编码支持

## 20210820

【Web】新增编辑预设功能

【Web】优化编码参数显示效果，原来是JSON，现在改为了格式化后的表单

【Web】优化了视频裁剪策略，支持单独设置开始时间、视频长度、结束时间

【Web】优化了视频裁剪设置的UI

## 20210821

【Web】新增支持Host设置开机自启，支持设置工作目录为当前目录

【Core】【Web】新增自定义参数任务

## 20210822

【Core】【Web】分离普通转码和视频拼接，普通转码输入多个文件时将输出多个文件。

【Core】【Web】转码的输入裁剪将和输入文件关联，而不是输出参数

## 20210823

【Web】改成了使用数据生成的菜单

【Web】做了主页

## 20210824

【WebAPP】新增由WinForms包装的可一键打开的桌面程序入口

## 20210825

【WebAPP】优化了WebApp：

- 退出时若在执行任务则禁止关闭
- 改为F12打开开发者选项
- 浏览器改为中文

【Web】新增支持从查看某一个任务的所有日志

## 20210826

【Core】【Web】接入了MediaInfo，查询媒体信息时可以查看更详细的信息

## 20210827

【Web】支持了选择子文件夹内的输入文件

【Core】将进程处理方法换成了自己的，更加好控制

## 20210830-0906

【WPF】基本完成了WPF版本

## 20210905

【Web】修复了启用裁剪的任务加入队列后，新的任务虽然仍然勾选裁剪时间，但实际设置无效的BUG

【Web】优化时间裁剪设置，解析文本框保持可用，解析后自动启动该项裁剪

【Web】设置输入文件后将不会自动应用输出文件，而是在输出文件为空时，认为输出相同名称的文件。

## 20210908

【WPF】新增从任务列表中查看选中任务的日志

【WPF】新增日志根据类型进行筛选

【WPF】新增拖放加入文件

## 20210909

【WPF】修复了状态面板在有任务执行时反而显示无任务的BUG

【WPF】修复了视频比较不可用的BUG

【WPF】做了一些优化

## 20210911

【WPF】新增文件拖入主窗口即可新增任务

【WPF】新增任务栏进度条

【WPF】修复了一些新增任务时等参数面板的BUG

【WPF】修复了在任务进行时新增任务会报错的BUG

【WPF】新增ffmpeg和ffprobe命令的检测

【WPF】新增支持打开输入文件和输入文件的目录

## 20210912

【WPF】优化时间裁剪操作，输入框将保持可用，并与选择框联动

## 20210913

【WPF】修复了编辑完参数后，编辑框未失去焦点导致没有更新源数据的BUG

【WPF】修改任务界面，仅显示处理中任务的进度条，其他任务显示时间

## 20210919-20

【WPF】新增媒体信息查询

## 20210920

【WPF】新增平滑滚动

## 20210927

【WPF】修复了拼接时部分场景下报错的BUG

【WPF】修复了不可修改文件数目的任务类型仍可新增任务的BUG

## 20210930-1001

【WPF】新增视频可视化裁剪

【WPF】修改新建任务面板不再为主窗口的子窗口

## 20211002

【WPF】视频可视化裁剪新增快进和快退按钮

【WPF】优化新增任务，解决了新增时的卡顿，新增任务的同时开始队列改为切换按钮，新增后将切换到主窗口

【WPF】修复了更新预设无效的BUG

【WPF】新增删除预设功能

【WPF】修改了运行时关闭窗口的逻辑，当有任务正在进行中时，点击关闭按钮将会最小化到托盘而不询问。并取消了最小化到托盘按钮

【WPF】新增设置界面，可以设置平滑滚动和远程主机

【WPF】新增添加任务到远程主机功能

## 20211003

标注了各条日志所属的项目

【Web】修复了新增拼接、合并等任务时输出不可为空、不会检测输入文件存在性等BUG

【WPF】优化了任务详情显示

【WPF】新增创建任务后清空文件、创建任务后关闭窗口选项

【WPF】视频裁剪新增使用Ctrl和Shift切换快进快退的长度等，新增帧率显示

## 20211004

【WPF】优化了任务列表的显示，仅显示进行中的、排队中的任务以及其它在本次程序开启后的任务（类似Adobe Media Encoder

【WPF】新增所有任务显示窗口，支持分页

## 20211005

【WPF】修复了新增所有任务窗口部分按钮不隐藏的BUG

## 20211007

【WPF】修复了任务列表下方按钮点击报错的BUG

## 20211121

【WPF】调整了主界面大小等

【WPF】修复了帧率参数缺失的BUG

【WPF】新增预设窗口，支持删除、导入、导出

## 20211205

【Web】修复了文件名过长时，文件选择框中的其他文件选择不全或不显示的BUG

## 20220109

升级到了.NET6

所有的生成文件设置为统一的目录

新增生成Web包的批处理

## 20220122

【Core】（应该）修复了暂停后显示的剩余时间会边长的BUG

## 20220123

更新了部署方式，支持一建生成发布和部署。

【WPF】修改了音视频输出选项，设置更方便

【Core】【WPF】新增支持手动设置视频或音频的编码器

【Core】【WPF】新增支持设置视频像素格式

【Core】【WPF】新增支持画面比例设置

【Core】【WPF】修改分辨率修改为文本型，可以设置除了基础的i*j之类的形式（Web版本暂时未改，导致无法使用）

【WPF】修改了一些UI细节

【WPF】新增查看输出参数功能

【Core】【WPF】新增支持设置每一类的默认预设，打开时将会自动加载该类型的默认预设

## 20220131

【Web】适配了后端，新增了自定义分辨率、画面比例和像素格式设置

## 20220205

【WPF】新增拖放文件到主窗口时，弹出选择对话框，可选择加入任务或查询信息

【WPF】修复了添加任务错误，但仍然会切到主窗口的BUG

## 20220208

【Web】新增导入导出预设的功能

【Web】修复了含有Token时无法上传文件的BUG

## 20220210

【WPF】修改为单窗口应用，左侧为固定的任务列表和进行中的任务信息区域，右侧为TabControl。

## 20220307

【WPF】修复了设置默认预设后不起作用的BUG

## 20220309

【WPF】新增记住上次任务参数的功能

## 20220310

【WPF】完成了默认输出目录的设置界面，但还未进行实现

## 20220311

【WPF】修复了打开新建任务页面时闪退的BUG

## 20220313

【WPF】修改新建任务时，分开设置输出目录和输出文件名

【WPF】完成了默认输出目录的逻辑

【Core】修复了进程结束后仍然有输出，导致无法捕捉到视频对比结果的BUG

## 20220319

【WPF】尝试使用多进程的方式规避ModernWpf库和FFME库冲突导致的视频裁剪界面崩溃的BUG

## 20220325

【WPF】优化了视频分割，使用单个程序集，并更改为模态窗口

【WPF】修复了切换Tab时会重复初始化的BUG

## 20220415

【WPF】修复了删除所有预设后界面被禁用的BUG

## 20220416

【WPF】修复了部分无法获取长度的视频或脚本（avs）处理时报错的BUG

## 20220424

【WPF】修复了应用的记忆或预设，视频或音频策略为“复制”时，若切换到“重新编码”后，参数设置无效且创建任务报错的BUG

【WPF】修复了建立副本时，输出目录错误的BUG

## 20220430

【WPF】修复了任务列表中的进度处于IsIndeterminate状态的BUG

## 20220502

【WPF】新增编码性能测试功能

## 20220503

【WPF】优化编码性能测试功能（去掉了勾选框、增加了子进度条、不导出音频等）

## 20220504

【WPF】修复了编码性能测试VP9报错的BUG

【WPF】修复了VP9无法跑满全部核心的BUG

【WPF】优化编码性能测试的得分算法，支持不全部测完也能获取得分

【WPF】新增记忆窗口最大化状态

【WPF】取消预设的“应用”按钮，改为点击预设卡片应用

【WPF】修复了浅色模式下Tab头显示不正确的BUG

## 20220710

【Web】新增远程关机、队列完成后自动关机功能

## 20220714

【Web】新增显示CPU占用率功能

## 20220826

【WPF】修复了拖放文件新建任务时，输入文件的数量可能小于允许的最小文件数量的BUG

## 20220902、06

【Core】【Web】新增队列计划开始功能

## 20221113

【WPF】修复和优化了一些界面问题

【Core】优化了运行错误的显示

## 20221126

【WPF】【Web】优化支持了WPF提交给远程的文件支持在输入目录中的子目录中

## 20221203

【Web】修复了远程提交的作业输出文件名错误导致执行失败的BUG

【WPF】修复了界面错位的BUG

## 20221210

【Web】修复了远程提交时，若输出文件名为空，会出错的BUG

## 20221217

【WPF】优化了预设管理界面，可以在预设管理处修改预设值、设置默认预设

## 202301040

【WPF】修复了拖放超过目标类型最大支持的文件数量的文件后报错的BUG

## 20230107

【Code】修复了部分FFmpeg输出无法识别导致无法显示进度的BUG

【Code】【WPF】【Web】新增支持H264和H265的二次编码

## 20230109

【Core】修复了视频拼接时无进度条的BUG

【Core】修复了目标文件名尾部带空格时出现错误的BUG

【Core】优化了错误信息的提取

【Core】优化了日志的数据库写入，加快了FFmpeg大量输出时的速度

## 20230123

【Web】修复和优化了一些Web界面问题

【Core】【WPF】新增AV1支持（未完全）

## 20230124

【Core】【WPF】优化了编码测试和AV1支持

## 20230125

【Core】优化了AV1的支持，新增SVT-AV1编码器的支持

【WPF】新增AV1支持

【WPF】优化编码测试

【Core】修复了视频比较不显示进度的BUG

【WPF】新增FFmpeg输出命令行

## 20230127

【Core】为增加灵活性，摆脱使用FFMpegCore模块的依赖，重构FFmpeg参数生成模块，手动生成FFmpeg参数。

## 20230128

【WPF】修复了编码测试码率不可修改的BUG

【Web】修复了显示超过1个小时的时长进度时，显示出错的BUG

## 20230202

【Core】优化给VP9和AV1自动加上支持多线程的参数

【Core】新增VMAF编码评价指标

【Core】修复了一些BUG（SVTAV1不支持最大码率、BufferSize设置有误、x264和x265的最大预设值错误、部分多文件任务进度异常等）

【WPF】编码测试新增VMAF，支持了配置记忆

## 20230203

【WPF】编码测试新增CPU占用率指标

【WPF】优化编码测试CPU占用率和FPS指标

## 20230207

【Core】新增明确SVTAV1不支持2Pass

【Core】优化了2Pass时的进度条显示，不会再分两次

【WPF】优化了勾选开关后，将自动赋默认值

## 20230208

【WPF】优化了修改视频编码后，将自动赋默认值

## 20230221

【WPF】编码测试新增CRF模式

【WPF】编码测试新增码率补偿功能

【WPF】编码测试新增能够显示和导出输出文件的大小

## 20230222

【WPF】编码测试支持了重复利用素材，编码时输出文件名将附带相关信息，不再覆盖原有输出。

## 20230224

【Core】【WPF】新增支持图像序列帧转视频的功能

【Core】【WPF】新增支持输入参数中的帧率和自定义参数

【Core】优化当输出文件名存在非法字符时，自动删除非法字符

## 20230228

【Web】新增支持图像序列帧转视频的功能，但未测试，因为还未实现自动生成图像序列通配文件名功能

## 20230302

【Core】【WPF】新增根据输入文件猜测是否存在图像序列，并自动重命名

## 20230303

【Core】【Web】新增自动重命名图像序列

【Host】新增连续输出FFmpeg进度信息时，将覆盖上一条进度信息（仅当输出内容为一行时）

## 20230317

【Core】【WPF】【Web】彻底删除Ts中转方式的视频拼接

【WPF】修复了几个没有显示正确标签的BUG

【WPF】优化在新增任务界面切换任务类型时，能够保留文件

【Core】【WPF】【Web】修复了拼接时，若指定了输出文件名但未指定后缀名时报错的BUG

## 20230322

【Core】修复了二次编码因没有指定Format导致Pass=1时失败的BUG

## 20230323

【Core】【WPF】修复了剩余时间大于1天时，显示不准确的问题

【WPF】新增显示已用时间

## 20230405

【Core】【WPF】【Web】新增支持二次压制时，两次Pass采用不同的速度预设

## 20230406

【Core】修复了H265编码二次压制没有起作用的BUG

【Core】【WPF】【Web】由于发现问题，回退前一天的更新内容

【WPF】修复了已用时间和剩余时间的小时部分被四舍五入的BUG

【WPF】对状态框进行了一些UI修改

【WPF】为需要终止任务的按钮添加了二次提醒，并将取消任务改为异步

## 20230407

【Core】【WPF】重构了获取媒体信息的方法，全部使用`MediaInfo.exe`进行。同步修改了WPF界面

【Web】修改了状态条样式

## 20230408

【Web】对媒体信息页面进行了同步修改

【Core】为数据库增加了一些索引并完成数据库迁移功能，大大加快了读取速度

## 20230409

【Core】【WPF】新增支持进程优先级设置

【Core】尽量避免日志保存失败导致的程序崩溃

【Web】为Host增加了文件日志和全局异常捕获

## 20230411

【WPF】优化界面布局，当没有显示任何标签页时，将修改任务面板和状态面板的位置以优化显示效果

【WPF】支持在转码时显示缩略图（测试）

## 20230414

【WPF】修复了启动时若存在待处理任务，会导致堆栈溢出的BUG

## 20230415

【WPF】修复了弹出顶级面板时，任务状态面板会位于其上方的BUG

## 20230416

【WPF】优化在转码时的缩略图显示

## 20230509

【Core】新增支持同步输出文件的修改文件为输入文件的修改时间（暂无设置UI）

## 20230515

【Core】【Web】网页端新增支持进程优先级设置

【WPF】修复了完整状态面板与底栏重合的BUG

## 20230524

【Core】【WPF】基本实现了读取H265视频主要编码设置并应用到当前转码设置的功能：通过将文件拖放到视频编码设置上方。

## 20230528

【Core】新增H264的读取视频主要编码设置并应用到当前转码设置的功能

## 20230623

【Core】【WPF】【Web】优化同步文件修改时间功能为任务级别参数

## 20230718~19

【Web】新增支持转码时的缩略图显示

## 20231211

【All】更新到.NET8，更新了所有Nuget

【WPF】修复了因FFME.Windows不支持最新版本的ffmpeg导致无法裁剪视频的BUG（https://github.com/unosquare/ffmediaelement/issues/642）

【WPF】去除了WindowsAPICodePack的依赖，改为.NET8自带的文件选取器

【WPF】由于zgabi/ffmediaelement貌似修复了死锁问题，因此将裁剪功能修改为单进程运行，加快了启动速度

## 20231215

【WPF】修改提交任务并开始队列由开关改为独立按钮

## 20240404

【Core】【WPF】新增处理后删除输入文件的功能

【Core】修复了最新版本ffmpeg因输出状态改变导致部分场景（如复制流）下无法获取进度的BUG

【WPF】由于发现裁剪闪退问题为解决，因此回退了[cc72816](https://github.com/autodotua/RemoteFFmpegGUI/commit/cc7281699db1d342262676efa8abeb4c92a19a72)

## 20240413

【Web】新增处理后删除输入文件的功能

## 20240504

【WPF】任务列表支持了多选后批量操作

## 20240505-21

重构，全局依赖注入，WPF完全MVVM，引入CommunityToolkit.MVVM。

## 20240526

【Web】修复了多选取消任务无效的BUG

修复了多个dbContext实例导致数据不同步的BUG并修改Logger为静态类

## 20240618

【WPF】修改了创建任务的按钮逻辑，支持了创建远程任务并启动队列

## 20240901

【Web】将Host的Windows Service方式与Console方式分离，更好地实现开机自启

## 20240903

【WPF】新增支持批量添加文件或添加文件夹内所有文件
## 2025-2026（v2 分支 master_v2）

【v2】重构 WebAPI：移除 Host/NamedPipe/Furion，WebAPI 直接执行 ffmpeg（原生 ASP.NET Core 10 minimal hosting）；数据库 v1.1→v2.0 自动迁移（DatabaseMigrator）；前端改为 Vue 3 + Element Plus + Vite。

## 20260818-19（v2 代码审查与修复）

按 [审查报告](docs/code-review/2026-08-18-v2-review.md) 与 [修复计划](docs/plan/2026-08-18-v2-fix-plan.md) 完成批次 0~5：

- 【FzLib 双轨】WPF 内化老版 FzLib 源码（`SimpleFFmpegGUI.WPF/FzLib/`，命名空间改为项目自己的），其余项目改用 NuGet FzLib 3.4.0-rc.2；libs 删除 5 个 FzLib 相关 DLL；删除 OneDrive 路径机制。
- 【功能】修复 Mux 任务被误建为 QualityCheck；队列 cancelQueue 永久置位；默认进程优先级接线（ConfigService）；预设编辑 409；Bearer undefined；processedOptions 字段名；Mux 长度处理（-shortest）死控件；默认输出路径落 OutputDir；预设导入 Type=3→Custom(99)；MediaInfo 编码设置缺键 500；迁移保留 Configs 用户配置；SnapshotSize 恢复为配置项。
- 【WPF】手动启动托管服务（DbLoggerService/AppLifetimeService）；远程主机协议适配（Bearer 前缀、Parameter 字段名、相对路径）；任务管理器并发字典；SyncModifiedTime/取消 NRE 防御。
- 【安全】路径穿越与绝对路径访问收口（FilePathHelper）；出厂 Token 清空与目录改为相对示例；AppActionFilter 异常统一 500 不回显详情；批量接口/上传/导入空引用与参数校验；快照临时文件用后即删；生产环境 UseExceptionHandler + Serilog 接入 host 管线。
- 【测试】WebTest 基建修复（ffmpeg/测试视频自动复制、19→40 个用例全绿）；迁移测试真实构造 v1 库验证；固定 sleep 改轮询。
- 【文档】README/CLAUDE.md 全面更新至 v2 现状；构建脚本适配（删 Host 发布、加 Windows 服务脚本）；清理死包与死代码。

## 20260819（WPF 退出死锁修复）

【WPF】修复主窗口关闭后进程无法退出：`Application_Exit` 在 UI 线程同步 `StopAsync().GetAwaiter().GetResult()`，而 `DbLoggerService.ExecuteAsync`（BackgroundService）启动时捕获了 Dispatcher 同步上下文，其循环延续被 post 回 UI 线程队列、恰被同步阻塞的 UI 线程卡住 → 死锁。Core 侧所有 await 补 `ConfigureAwait(false)`（DbLoggerService/AppLifetimeService/ConfigService，WebAPI 无同步上下文不受影响）；WPF 退出停止托管服务改为 5 秒超时 `Wait` 兜底。

## 20260912（实时推送与轮询泄漏修复）

【Web】修复前端定时器泄漏：任务页 3 秒轮询、状态栏 10 秒快照轮询（每跑一次队列多一个，且拉的是全站最贵的图片）、电源页 5 秒 CPU 轮询、任务页 8 处一次性补刷定时器，全部登记句柄并在卸载时清理。跨组件共享状态（队列状态、主题、窗口宽度）迁移到 pinia（`stores/queue.ts`、`stores/ui.ts`），同一条 `GET api/Queue` 不再被顶栏与任务页各轮询一次。

【Core】新增单例任务/队列变更通知器 `ITaskChangeNotifier`（`Events/`），在任务增删改、被队列置为进行中、队列起止、计划时间设置/取消等 12 处调用。WPF 进程内直连 Core、不订阅它，行为不变。

【WebAPI】新增 SignalR 实时推送：Hub 挂在 `/api/queue-hub`（只走 WebSocket，挂 `/api` 下是为了避开 SPA 回退把握手变成 200 + index.html）；`RealtimeBroadcaster` 事件驱动之外每 1 秒采样一次队列状态，推送瘦身 DTO（约 400 字节，原样推完整状态是 1.5~3 KB、1 秒一条≈10 MB/小时）；新增 `GET api/Queue/State` 供降级轮询一次拿齐状态/待处理/计划时间；Hub 鉴权由 `Program.cs` 内联中间件负责（Cookie 优先、access_token 兜底，规则与 `AppActionFilter` 一致，失败记 IP）。快照接口新增字节缓存：同一视频的同一播放时间点只跑一次 ffmpeg（并发同键共用一份结果、LRU 淘汰、失败不缓存）。

【Web】新增 `utils/realtime.ts`：连上就只靠推送（服务端空闲不发消息，靠 SignalR 自己的心跳保活）；重连序列 2/4/8 秒用尽后**永久**降级为 HTTP 轮询（处理中 3 秒/空闲 15 秒，页面不可见时暂停、切回来立即补一次），只有刷新页面才会重新尝试 WebSocket；顶栏新增四态连接圆点（绿=推送已连接/黄闪=断开重试中/蓝=轮询兜底/红=取状态失败，悬停有文案）。状态带自增序号且只接受更大的序号，避免降级与推送并存时旧数据覆盖新数据（序号从毫秒时间戳起步，服务端重启后不会让前端永久丢弃状态）。

【Web】任务列表不再定时轮询：处理中靠 1 秒推送更新进度，列表由"任务清单变了"的推送事件驱动刷新（去抖 200 毫秒）。

【Web】修复实测发现（浏览器 + 真 WebAPI 手工跑队列）的两个时序问题：① 点"开始队列"后**推送往往比 HTTP 响应先到**（队列已跑完、甚至任务已结束），此时才做的乐观更新会把已经更新的真实状态盖回 `isProcessing: true`，而且之后不会再有推送来纠正——底部状态栏会一直停在"处理中"。改为随命令带上"发起前的状态序号"，若期间已有更新的状态就放弃这次乐观更新；② 管理器已建立但 ffmpeg 进程尚未启动的瞬间，状态里的 `isProcessing` 为 false，会让状态栏收起再弹出，改为"有管理器就算在处理"。

【All】按两轮独立子 Agent 复核的意见修复：① 把"读状态 + 分配序号"收进同一把锁——否则旧状态可能拿到更大的序号，而前端只认更大的序号，会把旧状态当最新，队列结束后界面永久停在"处理中"（推送与 HTTP 两条路都有这个缝）；② 每秒采样循环补 try/catch：默认 `BackgroundServiceExceptionBehavior=StopHost` 下，推一条状态失败会把整个 Web 服务停掉；③ 事件推送改到线程池执行（`Notify` 是同步遍历，异步方法在被第一个 await 拦住前一直跑在队列/请求线程上，含一次网络写和一次数据库查询）；④ 广播复用 Hub 自己的 JSON 选项（不再另建一份靠注释保持一致）；⑤ Hub 鉴权补 `Authorization` 头分支，失败日志带凭据来源；⑥ 快照缓存移到 `Core/Caching/` 并补 5 个单测（并发同键只跑一次、失败不缓存、超容量 LRU 等）、键改为整数；⑦ 降级轮询：`hasPending` 变化也刷新任务列表、加 `hintProcessing` 让乐观操作立刻切到 3 秒节奏、`Queue/State` 单独加 10 秒超时、修 `connect/close/scheduleNextPoll` 三处生命周期缺口。UnitTests 190 例、WebTest 受影响的 30 例、前端 `npm run build` 全部通过。

## 20260912（WPF 运行状态边框贴边）

【WPF】主窗口的"运行中"边框（运行中=绿色闪烁、全部暂停=橙色稳定）原先只围住标题栏以下的内容区，且**底部命令栏那一行始终在框外**：`RunStatusBorder` 只写了 `Grid.ColumnSpan="99"`、没有 `Grid.RowSpan`，实际只占据 `grdMain` 的第 0 行（任务清单 + 状态区）。补上 `Grid.RowSpan="99"`，让它覆盖内容区的所有行。

【WPF】四条边要贴到**窗口可见区域**的边缘（含标题栏那一条），新增 `MainWindow.UpdateRunStatusBorderMargin()`：挂在 `grdMain.LayoutUpdated` 上，按内容区相对窗口可视树的实测偏移（`TranslatePoint`）反推 `Margin`。两个不能写死的量：① 库的窗口模板把窗口内容放在标题栏下方的那一行，实测偏移为 `(1, 37)` DIP（1=窗口自身边框、36=标题栏）；② 最大化时库的 `MaximizedWindowFixer` 会把 `Window.Padding` 设成 6.5 DIP（窗口比屏幕可见区大一圈，靠它把内容推回可见区），所以目标取"窗口边界 − Padding"（还原时 Padding 为 0，同一公式成立）。高对比度主题下不补偿：库会给内容加 7px 外边距并画一条同厚的窗口边框，贴到最外沿反而会被压住看不见。

【WPF】按子 Agent 复核意见修复（5 条全部属实）：① 右/下边距符号写反——`Margin` 四边统一是"正值=向内收"，右/下应取"内容区远端 − 目标"，写反后实测右、下两边各内缩 2 DIP；② 高对比度分支；③ 构造函数里那次 `UpdateRunStatusBorderMargin()` 调用必被零尺寸守卫拦掉，删除，首次定位交给第一条 `LayoutUpdated`；④ `LayoutUpdated` 改用具名方法并在 `Closed` 里退订，与同处其它订阅的写法一致；⑤ XAML 注释里"覆盖整个窗口"的归因修正（Span 只保证覆盖内容区，贴到窗口边缘靠位置补偿）。

【验证】200% DPI 下截屏逐像素量（测试用仓库 Debug 输出的一份独立副本 + 独立临时目录/数据库，临时强制显示边框后已还原）：还原状态与最大化状态四边均为 0px，即 2 DIP 的框线正好压在窗口可见边缘上；修前右、下为 4px（内缩 2 DIP）。

## 20260916（WPF 缩略图点击放大预览）

【WPF】任务卡片上的快照缩略图（360×256）以前只能静态显示，而任务运行中每秒左右换一张图，细节完全看不清。现在点击它**弹出一个独立窗口**，可滚轮缩放（以光标为锚点）、左键拖动平移、双击在"适应窗口 / 1:1"之间切换、Esc 关闭；工具栏另有缩小 / 1:1 / 适应窗口 / 放大四个按钮，并显示缩放百分比与图片像素尺寸。方案见 [docs/plans/2026-09-16-WPF缩略图点击放大预览.md](docs/plans/2026-09-16-WPF缩略图点击放大预览.md)。

新增通用图片预览面板 `Panels/ImagePreviewPanel.xaml(.cs)`（只依赖"一张位图 + 一个标题"，别处要看图可直接复用）+ `ViewModels/ImagePreviewPanelViewModel` + 纯数学 `Panels/ImagePreviewMath`（锚点缩放 / 适应 / 1:1 / 平移钳制，不引用任何控件），宿主仍走 `ViewWindow`（不直接 `new Window()`，避免 iNKORE backdrop 递归）。点击入口在 `Panels/StatusPanel.xaml`；快照尚未生成时（`Snapshot.Source` 为 null）光标与提示恢复成"不可点"的样子、点击静默返回，不做"先撒谎再道歉"。

几个关键取舍：① 窗口打开即**冻结当帧**，解码用 `BitmapCacheOption.OnLoad` + `Freeze()`，与文件彻底解耦——主界面那张图仍锁着同一个文件、临时文件将来被清理，都不影响已打开的窗口；② **同一帧重复点击只激活已开窗口**（按快照文件路径去重，窗口关闭时移除），不同帧/不同任务各开各的窗口；③ 初始缩放是"适应窗口但不超过 100%"（小图不放大），平移做钳制（图小于视口时居中不可拖、大于时偏移夹在边缘内），因此不存在"图被拖丢"的状态；④ 图片区背景固定近黑、不跟随主题（亮色主题下浅灰底会让缩小的图与背景糊在一起），缩放全程高质量平滑插值。

【已知】WPF 的快照临时文件（`%TEMP%\SimpleFFmpegGUI\snapshot\`，每张快照是一个新的 GUID 文件名）仍然是**只增不删**，本次未处理：删除时机与主界面那张正锁着文件的图冲突，需要单独设计，另案处理（该问题原记录见 `docs/code-review/2026-08-18-v2-review.md`）。

【复核】按子 Agent 复核意见修复（报告 10 条，9 条属实并已修复，1 条记为取舍）：

- **致命（已实测复现）**：`LoadImage` 里 `CreateOptions = IgnoreImageCache` 配 `StreamSource` 会让 `BitmapImage.EndInit()` **必定**抛 `ArgumentNullException: Value cannot be null. (Parameter 'key')`——StreamSource 路径下 BitmapImage 内部的 `_uri` 是 null，而 IgnoreImageCache 会无条件拿它去 `RemoveFromCache`。后果是每次点击缩略图都走到 catch 弹"读取快照失败"，**预览窗口永远打不开**。用独立探针程序在 .NET 10.0.12 / PresentationCore 10.0 上实测：当前写法抛异常、去掉该选项正常（320x180、IsFrozen=True）、加载完删掉文件后仍能读出像素（说明 OnLoad + Freeze 已足以与文件解耦）。已删除该选项，并在代码与计划文档里写明为什么不能加。
- 【WPF】标题的时间点改用 `SnapshotViewModel.Time`——新增该属性，由 `UpdateSnapshotAsync` 在写入 `Source` 的同一处记下**抓帧时刻**。原来用点击瞬间的 `ProcessStatus.Time`，而快照最多十秒才刷新一次（`PeriodicTimer(10s)` + "与上一张差不到 1 秒就跳过"），标题描述的是另一帧；同时小时数改为"天数×24+小时"，`TimeSpan` 的 `hh` 是 0~23 的小时分量，素材位置超过 24 小时会回绕（与 `HourMinSecTimeSpanConverter` 的处理保持一致）。
- 【WPF】读文件 + 解码移到后台线程（`Task.Run` + `Freeze` 后回 UI 线程）：解码耗时由设置页那个**没有范围校验**的"快照尺寸"决定（默认 1080P 约几十毫秒，填成 2160P 就是几百毫秒），同步做会让点击之后界面僵一下。`Show` 相应改为 `ShowAsync`，点击处理器改 `async void` 并包 try/catch（`async void` 里逃出去的异常在 Debug 下没有全局兜底，会崩进程）。
- 【WPF】开窗那一段补 try/catch 并在失败时清掉去重表条目：原来 `openedWindows[key] = window` 发生在 `Show()` 之前，而唯一的移除路径是 `Closed`，一旦 `Show()` 抛异常（owner 正在关闭、主题资源或 HwndSource 失败等），静态表里会**永久**留下一个窗口引用连同那张几 MB 的冻结位图。
- 【WPF】点击处理器的判断条件与 XAML 的 DataTrigger（只看 `Snapshot.Source`）对齐，不再额外要求 `ProcessStatus` 非 null：任务出队时 `ProcessStatus` 会先被置 null、再等一次数据库往返，那段时间卡片仍在屏幕上且显示着手型光标，点下去却静默返回——正是"UI 先撒谎"的情形。另补 `e.ClickCount != 1` 直接返回（双击的第二下不必再走一遍）。
- 【Web】`preview-z-index` **不是** el-image 的属性（2.14.2 里叫 `zIndex`，且组件 `inheritAttrs: false` + 属性白名单会把它过滤掉），这一行从未生效，改为 `:z-index="9999"`。
- 【Web】预览层改为绑定"按下鼠标那一刻冻结的帧"副本（`@mousedown="freezePreview"`），而不是直接绑 `snapshotSrc`：① 上一批改动加的 `preview-teleported` 让预览层挂到了 body，不再受缩略图容器 `v-show` 的影响——快照请求一旦失败（catch 分支把 `snapshotSrc` 置空）就会在整站上留下一层显示"加载失败"、点遮罩也关不掉的浮层（el-image 只暴露 `showPreview`，没有关闭预览层的 API，`hideOnClickModal` 默认为 false）；② 即便请求一直成功，10 秒一次的轮询也会把用户正放大细看的那一帧在脚下换成新帧（Element Plus 拿 `urlList[activeIndex]` 当 `img` 的 `key`，会整张重建）。改完之后两端"点开看的就是这一帧"的语义也一致了。
- 【WPF】留作已知取舍：① 标题里的时间点是"**请求**取帧的位置"，而 `-ss`（输入 seek）+ `-skip_frame nokey` 会把实际帧落到 ≤ 该时间的最近关键帧上（最大误差一个 GOP），要消掉得改取帧方式，不在本次范围；② `Source` 非 null 但文件被外部清掉时，点击会得到一条错误提示而不是静默失败（比"点下去没反应"好，不另做探测）。

【再复核】第二轮专门针对"修复本身有没有引入新问题"复核（时序/生命周期/并发/资源释放四条线）：结论是修复没有引入实质缺陷，另确认了 Web 端 `frozenPreview` 方案成立（mousedown 与 click 之间 Vue 必定把赋值刷进组件，触屏的合成事件序列同样包含 mousedown）、跨线程位图的安全性来自 `Freeze`（对照组：后台创建但不 Freeze 的位图在 UI 线程一碰就抛）、连点两次只会得到一个窗口（"检查 → 登记 → Show"之间没有 await）。据此又收口三处：① `Apply` 改为返回"变换是否真的变了"，只有真变了才退出"适应窗口"状态——原先缩放到上下限时 `ZoomAt` 原样返回、调用方却已经把状态置成了自由模式，留下"变换仍是适应那一套、状态却不是"的不一致（触发需要快照某一维超过视口约 17 倍，默认配置到不了，但那个设置框没有范围校验）；② 开窗失败的清理只删"确实登记过的那一个"，去掉按 key 无条件删的危险语义；③ 给 `ShowAsync` 注明必须在 UI 线程调用（await 之后要靠同步上下文回 UI 线程建窗）。

【验证】`dotnet build SimpleFFmpegGUI.sln`（0 错误）与前端 `npm run build`（含 `vue-tsc`）通过；解码路径用置于仓库外的独立探针程序在 .NET 10 运行时上实测；鼠标手势部分由手工点验。

## 20260917（WPF 预览窗口：图片缩成左上角小图 / 工具栏按钮底色）

【WPF】修复上一版遗留的两个问题：① 预览窗口里的图**缩成一小块贴在左上角**，百分比与像素尺寸却都对（55%、1920×1080），且怎么拖都不动；② 底部工具栏的按钮常态带着一层底色（按钮与图片区分属 `Grid` 的两行，压不到图上，那是工具栏那一行里自己的底色）。

【WPF】① 的根因不在数学，而在**布局**：WPF 会给"期望尺寸被父级槽位压小"的元素**自己**挂一层**布局裁剪**——`DesiredSize` < `RenderSize` 时，`LayoutInformation.GetLayoutClip` 返回的就是父级给它的那个槽位矩形。1920×1080 的图放进 1098×597 的视口，`DesiredSize` 被压成 1098×597、`RenderSize` 仍是 1920×1080，于是只画得出整帧左上角那一块（约占 57%×55%），**再**被矩阵按"整图 1920×1080"缩放一次 → 可见内容 = 1098×0.5528 × 597×0.5528 ≈ 607×330 贴左上角。而 `img.Width/Height`、`ActualWidth`/`RenderSize`（都报 1920×1080）、矩阵、百分比、以及 WPF 自己算出来的"图片在视口里的矩形"（18.3, 0, 1061, 597）**全都显示"正确"**——这才是上一轮复核没抓到它的原因：**属性报的尺寸与真正画出来的尺寸不是一回事**。

【WPF】这层裁剪**与父级的 `ClipToBounds` 无关**（实测：把视口的 `ClipToBounds` 从 True 改成 False，`img.LayoutClip` 仍是 `0,0,1098,617`、渲染包围盒一字不差）。所以别去改 `ClipToBounds`——真正起作用的是"让子元素的期望尺寸不被压小"，也就是套 `Canvas`；裁掉视口**之外**的部分才由外层 `Border` 的 `ClipToBounds` 负责（实测它的开关只影响"内容会不会画到视口外面去"）。

【WPF】这条机理我先后写错过**两次**，本条目保存的是订正后的说法：先以为位图被 `Stretch="Fill"` 压成视口形状（"1.78 被压成 1.84"），后以为裁剪是父级 `ClipToBounds` 干的。两次都是复核者用**带标记的图**（源图左上/中心/右下各放一个 100×100 方块）逐像素量标记落点否掉的：中心标记落在按 1:1 映射算出来的位置上（不是被压扁后的位置），说明**源图没有变形、只是被裁**；而关掉 `ClipToBounds` 结果不变，说明裁剪来自元素自己的布局裁剪（我自己也照这个办法量了一遍，四个组合的 `GetLayoutClip` 与包围盒都对得上）。我先前那两张截图同样印证"裁剪"：同一面墙在破损态里占画面宽的 57%、修复后占 32%，比值 1.78 ≈ 1/0.572，正是"放大裁剪"的特征（"1.84"其实是**可见区域**的宽高比）。**教训**：结论对不等于解释对——两次错的都是"为什么"，而写错的"为什么"会让后人去改错地方（改 `Stretch`、改 `ClipToBounds` 都是白改）；涉及渲染机理的描述，宁可只写"实测到的事实"，也别顺手给一个听起来顺的因果。

【WPF】复现与定位过程：先写独立探针（仓库外，`%TEMP%\wpfprobe3`：同样的 Border + 左上对齐 Image + MatrixTransform + 真实 `ImagePreviewMath`），A/B 两种写法各截一张图（`RenderTargetBitmap`），再用 `System.Drawing` 逐像素量"非背景像素的包围盒"——A（现状）= 605×334 贴左上角、B（Image 外套 `Canvas`）= 1069×590 铺满视口，一眼定案。随后把探针升级为**真实面板 + 真实 `ViewWindow` + iNKORE 主题资源**（`%TEMP%\wpfprobe4`，反射注入 `App.ServiceProvider`），量到 `img.DesiredSize = 1098×597`（改前）/ `1920×1080`（改后），与像素测量互相印证。**注意**：这类问题只看 `ActualWidth`/`RenderSize`/矩阵是查不出来的，必须截图像素级量。

【WPF】修法：把 `Image` 套进一层 `Canvas`（`imageHost`，`ClipToBounds="False"`）。`Canvas` 用**无限尺寸**测量子元素，`Image` 的 `DesiredSize` 因此恢复成 1920×1080（= `RenderSize`）、布局裁剪消失，矩阵（按图片像素尺寸算）与实际绘制才一致；视口之外的部分仍由外层 `Border` 的 `ClipToBounds` 裁掉。附带影响：视口自身的**期望**尺寸变成 0（`Canvas` 不参与尺寸计算），面板的 `DesiredSize` 从 1098×642 变成 318×45（只剩工具栏）——不再给宿主传一个巨大的期望值，但也意味着**宿主必须给面板尺寸**（`ViewWindow` 是显式窗口尺寸 + 内容 `Stretch`，满足），因此给视口加了 `MinHeight="120"`，兜住"按内容定尺寸"的宿主（只兜高度这一维）。XAML 里写明了"为什么必须套 Canvas"以及"这不是通用规则、别照搬到缩略图上"（`StatusPanel` 那个 `Border` 没有 `ClipToBounds`，缩略图 `Stretch="UniformToFill"` 的外溢——期望 455×256 > 槽位 360×256——正是靠**同一层布局裁剪**裁掉的，套上 `Canvas` 后裁剪消失，图会糊出卡片：复核实测包围盒 480×328）。

【WPF】② 四个工具栏按钮加 `Background="Transparent"` 与 `BorderBrush="Transparent"`：iNKORE 的按钮样式在常态给一层底色（复核实测：浅色主题按钮内 `#FBFBFB` vs 工具栏 `#F2F2F2`，很淡；暗色 `#373737` vs `#2B2B2B`，能看出方块），局部值只决定常态，悬停/按下/禁用态由主题样式打在模板子元素上、照常给反馈，效果是"平时只有一个图标，指上去才亮"。写具名的 `Transparent` 而不是 `{x:Null}`——null 会让按钮空白处不参与命中测试（复核实测：`{x:Null}` 时按钮空白处只命中到根 `Grid`，空白处点不动）。

【验证】`dotnet build SimpleFFmpegGUI.sln`（0 错误）；真实面板探针截图后逐像素量：内容包围盒 = x 18~1079、y 0~589，与矩阵预测的 (18.3, 0, 1061, 597) 一致（改前是 605×334 贴左上角）；按钮区域像素由不透明底色变为透明。临时探针目录在 `%TEMP%` 下，验完即弃。

【复核】改动后按惯例走了一轮"复核-修复-再复核"（第一轮两个独立子 Agent：一个盯布局/渲染，一个盯交互/时序/生命周期；第二轮专门复查"修复本身有没有引入新问题"）。结论：无致命问题，`Canvas` 修法与按钮改动都成立，事件路由/鼠标捕获/去重表/解码线程/缩放平移边界逐条核过；据此收口如下：

- **订正文档与注释里的错误机理**（代码本身是对的）：见上文的"裁掉 vs 压扁"、"布局裁剪 vs `ClipToBounds`"两段，以及按钮那条"叠在图片区上方"、`#DDDDDD`（那是**没套 iNKORE 主题**的 WPF 经典按钮底色，我那次探针没加载主题资源，不足以作依据）的措辞。
- **计划文档里的"布局约定"补限定**："`Image` 必须夹在 `Canvas` 里"不是通用规则，它只是让子元素的期望尺寸不被压小；并写明面板要求宿主给出尺寸。
- 【WPF】`Viewport_LostMouseCapture` → `EndDrag()`：拖动中被 `Alt+Tab`/UAC 抢走捕获、又在窗口外松开时，原来要等鼠标再次移进视口才自愈（光标卡在四向箭头）。复核确认不会递归（`EndDrag` 先置 `dragging = false` 再释放捕获）、也不会被工具栏按钮或加载环误触发（`LostMouseCapture` 是冒泡事件，只有视口自身及子树丢捕获才会进来）。
- 【WPF】`ShowAsync` 的 catch 里补 `WindowBusyOverlay.Unregister(window)` **并把窗口 `Close()` 掉**：`Show()` 抛异常时窗口从未 `Closed`，加载环那张静态表会一直留着条目。复核实测提醒了一件事——**只摘 `rings` 并不解开对象图**（从未显示的窗口本来就被 `Application.Windows` 强引用钉着），真正释放要靠 `Close()`，所以这里两件事都做（关窗同时把退订/Dispose 等正常收尾跑完，失败也只记日志）。

【复核】另有两条被**否掉**与两条**未采纳**：① "双击缩略图时第二下会落到新开的窗口上、`ClickCount` 仍记 2，于是窗口以 100% 打开"——不成立：`WM_LBUTTONDBLCLK` 由系统按**窗口**生成（`CS_DBLCLKS`）且只投给拿到第一次点击的那个窗口，第二下在新窗口上收到的是普通 `WM_LBUTTONDOWN`（`ClickCount = 1`）；② "按钮的 `DesiredSize` 会让面板塌陷"——在 `ViewWindow` 下不可达（属主窗口最小 400 DIP 高，视口最小也有 250+；已加 `MinHeight` 兜底，且复核实测量到"按内容定尺寸"的宿主里视口确实不再塌成 0）。未采纳：`MaxScale=16` 下极小图的显示（快照至少上千像素，不可能出现）、把"适应窗口时不可平移"改掉（当初确认过的取舍，改法已记在上面【说明】里，用户要求再加）。

【已知】复核顺带指出一处**同源的既有问题**（本次未改，也不因本次变差）：`ChildWindowManager` 里也是 `new ViewWindow(...)` + `Show()` 一整套没有 try/catch，失败时它自己的 `modelessWindows` 与 `WindowBusyOverlay.rings` 都会留下条目（同样的"从未 Closed 的窗口"问题）。另一条更早的已知问题（WPF 快照临时文件只增不删）见上一节。

【说明】"适应窗口"时图正好铺满视口、没有可平移的余量，所以此时拖拽本就不动（这是当初确认过的取舍：图不大于视口就强制居中，不会把图拖丢）；滚轮放大后拖拽平移一切正常。若希望"图比视口小时也能在视口内自由挪动"，把 `ImagePreviewMath.ClampToView` 里"小则居中"的钳制改为"夹在 `[0, 视口尺寸 − 图尺寸]`"即可，随时可加。

## 20260917（WPF 忙碌遮罩：覆盖面补全 + 文案）

【WPF】主窗口那个忙碌遮罩（`ProgressRingOverlay`，`MainWindow.xaml`）原来只写了 `Grid.ColumnSpan="99"`、**没写 `RowSpan`**，于是它只占 `grdMain` 的 Row 0（`*` 那一行）：底部那条 8 DIP 间隔行与 `CommandBar`（Auto 行）**从来没被遮罩盖住**——忙碌时最下面一条不暗、按钮还能点。它当时那点 `Margin="-8"` 往下溢 8 DIP 补的正是那条间隔行的高度（`RowDefinition Height="8"`），左右/上溢出的 8 属于顺手带的。现在补上 `Grid.RowSpan="99"` 并**去掉 `Margin`**：遮罩严格等于内容区，那条缝由 Span 覆盖，露出来的 1 DIP 窗口边框与 36 DIP 标题栏本来就不该被压暗。`-8` 是第一个引入该遮罩的提交 `898da34`（ModernWpf 时代）就带来的手调余量，与任何实测几何量都对不上（窗口边框 1、标题栏 36、最大化时 `Window.Padding` 6.5、高对比度下内容外距 7）。遮罩仍放在 `RunStatusBorder` **之前**：边框画在遮罩之上，忙碌时运行指示保持鲜亮（未最大化时两者重叠仅约 1 DIP）。

【WPF】加载环文案：`WindowBusyOverlay` 的 `SetBusy(bool)`/`SetNotBusy` 改成 `SetBusy(string message)`/`SetNotBusy()`，`WindowEnableMessage` 加可选 `Message`；各调用方在发 `false` 时说明这次在做什么（加入队列 / 提交到远程主机 / 裁剪 / 读取媒体信息 / 终止队列 / 测试连接 / 取消任务），显示在卡片「请稍等」下面那一行（`ProgressRingOverlay` 的那个 `Message` 行原本一直空着）。顺带一处防御：`SetBusy` 时若上一次的忙碌没配对收掉（并发、或某个调用方漏发 `SetNotBusy`），先把旧窗口的环收掉——否则 `SetNotBusy` 只关得掉最后那个窗口，旧窗口会一直变暗且点不动。

【验证】`dotnet build SimpleFFmpegGUI.WPF/SimpleFFmpegGUI.WPF.csproj -p:OutputPath=%TEMP%\wpfbuildcheck\`（程序正开着，走临时输出目录）：0 错误。

## 20260917（输入文件条：裁剪按钮挪到时间行，更多按钮挪到文件行）

【WPF】`FileIOPanel` 里每个输入文件的按钮原本是：文件行右侧 = 浏览文件 / **裁剪** / 移除当前，时间行右侧 = **更多**。裁剪是对下方那两个时间框的操作，却离得最远；"更多"展开的面板（图像帧序列 / 输入帧率 / 其他参数）又和文件行更相关。按"谁管谁挨着谁"对调：**文件行 = 浏览文件 / 移除当前 / 更多**，**时间行右侧 = 裁剪**（`Cut` 图标）。

【WPF】裁剪按钮不放回"更多"那一格而是放在同一行的时间框旁边；它自己的 `Visibility`（绑 `ShowTimeClip`）**删掉**了——整行已经按 `ShowTimeClip` 折叠，同一个条件判两遍没意义。它的可见范围没变（仍只在转码任务里出现）。

【WPF】**顺带的行为变化**：`ShowTimeClip` 只在转码任务为真，而"更多"原来住在那一行里 → **拼接 / 混流 / 质量测试 / 自定义这几类任务此前根本看不到"更多"按钮**，它管的三个选项也就够不着。现在按钮挪到文件行（不受 `ShowTimeClip` 管），所有任务类型都能展开。看着像是把一处"选项藏起来了"的隐性问题一并修了；若想保持原样，给这个 `ToggleButton` 也绑上 `ShowTimeClip` 即可。`grdMore` 与两条故事板都在同一个 `DataTemplate` 的命名域里，按钮换父级不影响 `Storyboard.TargetName` 解析（`grdMore` 在 `Grid.Row="99"`，按 Grid 规则落在最后一行那个 `*` 行上）。另外给它补了 `Height="32"`：它原来在 Grid 单元格里是被行高拉满的，进了按钮排得自己立住。

【验证】同上（`0 错误`，走临时输出目录）。视觉效果未跑起来看，按"能编译"收工。

## 20260918（忙碌遮罩的配对与计数、「更多」按钮的归属）

【WPF】接着上一节走了一轮"复核-修复-再复核"（两个独立子 Agent：一个盯布局/渲染，一个盯时序/生命周期；修完由**同一个** Agent 复查），查出并修掉四条：

1. **`ClipAsync` 会发出没有配对的"不忙碌"**（`FileIOPanelViewModel`）：两个提前 return（输入为空 / 文件不存在）在 `SendMessage(false)` **之前**，而 `finally` 无条件发 `true` → 在"输入框还空着就点裁剪"这类情况下会多发一条 `SetNotBusy()`，把**别的窗口**正在转的环收掉、那个窗口提前变回可点。改为 `busySent` 标志：发过才开始收。
2. **`busyWindow` 只有一个槽位、且不配对**（`WindowBusyOverlay`）：`SetBusy`/`SetNotBusy` 是**按操作**配对的（7 处调用点各一对），可环只有一个——两个操作重叠时，先结束的那个会把还在跑的那个的环关掉。改成计数：`busyCount++` / 减到 0 才收环；**已经在忙时不挪窗口**（挪窗口等于把原来那个窗口提前解锁），只有新操作落在同一窗口时把文案换成最新的。可达路径核实过两条：媒体信息窗口的 `initialize(view)` 在 `new ViewWindow(...)` 之前执行（`ChildWindowManager.cs:43-45`，`Register` 在 `ViewWindow` 构造函数里），所以第一次读元数据的环挂在主窗口上、窗口显示后再改一次输入就是第二个操作；以及"AddTask 子窗口加入队列 + 主窗口终止队列"这种跨窗口重叠。我上一版加的"先把旧窗口的环收掉"守卫治的是"环永远卡住不消失"，但代价是误收，已经删掉。
3. **环可能在忙碌中途凭空消失**（`Inkore.Extension/ProgressRingOverlay.xaml.cs`）：`Hide()` 先把 `IsHitTestVisible=false`，500ms 淡出结束才 `Collapse`；两个忙碌操作挨着来时（同一窗口内间隔 <500ms），淡出的那条动画 `Completed` 可能把正在淡入的环折叠掉。给 `Completed` 加了 `if (!showing)` 守卫——只有"最后一次调用确实是 Hide"才折叠。判定为 PLAUSIBLE（静态审查看不出 WPF 是否会对被替换掉的动画触发 `Completed`），属防御性修改。
4. **「更多」面板里的选项并非所有任务类型都生效**（`FileIOPanel.xaml`）：拼接的执行器自己造输入、只取 `FilePath`（`FFmpegTaskService.RunConcatProcessAsync:664-691`），「图像帧序列 / 输入帧率 / 其他参数」三样**全部被丢弃**；转码、混流、质量测试才真的把它们拼进命令行（`FFmpegTaskService.cs:574`、`:610`，`ArgumentsGenerator.GetInputArguments:69-79` 读 `Framerate`/`Extra`）。新增 `FileIOPanelViewModel.ShowMoreOptions`（这三类为真），ToggleButton 与 `grdMore` 都绑它。
5. **「更多」面板跟着按钮走**：按钮挪到文件行后，面板却仍在卡片最后一行（`Grid.Row="99"` 被 Grid 钳到最后一行），转码任务里两者被时间行隔开。把 `grdMore` 移到文件行正下方（`Grid.Row` 99→4），时间行 4→5，行定义改为 Auto/4/Auto/8/**Auto(面板)**/**Auto(时间行)**/`*`（原来"时间行后 8 DIP 间隔行"去掉、避免收起时出现双倍空档；末尾 `*` 与"如果是Auto会抖动"的注释保留）。

【说明】上面 1、2 两条的语义边界：计数解决的是"重叠操作互相收环"，但它仍然**不保证**环挂在"发起操作的那个窗口"上——`FindForegroundWindow` 取的还是**当时**的前台窗口，刚 `Show()` 的子窗口反而不受遮罩保护。更明确地说，"已经在忙时不挪窗口"是有意取舍：跨窗口重叠时，**后发起操作的那个窗口在整个重叠期间一个环都没有**（那边完全可交互），而先发起那个窗口会被封锁到全部操作结束——这等于把"环落错窗口"从偶发变成了系统性表现，要一并记进【已知】。彻底解法是"按窗口计数"（`Dictionary<Window,int>` 各自 Show/Hide），顺带能把问题 1/3/4 一起收掉，但那要点名目标窗口、属设计变更，本轮不做。

复核还确认了两件别在后续重构里弄丢的事：① `ClipAsync` 里那个 `busySent` **不能删**——`SetNotBusy` 的 `busyCount > 0` 下限拦不住"多发的 true"，它照样会减掉别人的计数，配对只能靠调用方自己保证；② `SetBusy` 里的 `busyCount++` 必须放在 `FindForegroundWindow()` 的 null 判断**之前**（初版放在后面，复核发现那一路"不计数却照样被减一次"会让配对失衡，已改）——计数的语义是"已经开始的忙碌操作数"，要与 7 处调用点一一对应。

【验证】`dotnet build SimpleFFmpegGUI.WPF/SimpleFFmpegGUI.WPF.csproj -p:OutputPath=%TEMP%\wpfbuildcheck\`：0 错误。视觉与手感未跑起来看。

【复核】仍按"两个独立子 Agent（布局/渲染 + 时序/生命周期）→ 我判断 → 修复 → **同一个** Agent 复查"跑完。复查结论：`ClipAsync` 的 `busySent`、淡出 `Completed` 守卫、「更多」按任务类型显示、面板挪位，四条均 **FIXED**；计数方案四种组合 × 两种结束顺序全部推演通过，与 `Unregister`（窗口关闭）组合也不失衡。复查另外逼出两处**我自己改出来**的问题，都已修：① `busyCount++` 原本放在 `FindForegroundWindow()` 返回 null 的提前 return **之后**——那一路"不计数"却照样被配对的 `SetNotBusy` 减一次，会把这次的配对失衡从后门放回来（今天不可达，但结构上是错的），已挪到判断之前，计数语义定为"已经开始的忙碌操作数"；② 删掉"时间行后 8 DIP 间隔行"后，面板展开时它内部的「其他参数」输入框与时间行贴成 0 DIP，已把间隔补在面板**内部**末尾（收起时整块被 `ScaleY=0` 压成 0，所以不会在文件行与时间行之间变成双倍空档）。

【复核】几条被**否掉**的猜测（记下来免得后人重查）：① "面板从 `*` 行挪到 `Auto` 行会触发抖动"——实测（同构 Grid 放进 `VerticalScrollBarVisibility=Auto` 的 ScrollViewer）证明该结构下 ScrollViewer 以**无限高度**测量内容，`*` 与 `Auto` 等价（收起 extent 98、展开 198，两种写法数字完全相同），原注释的顾虑在此结构里不成立，末尾那个 `*` 行留着无害；② "「更多」按钮与下一行裁剪按钮右边缘必然不齐"——不成立，两个 Grid 的最后一格都是 `Auto`，子元素右边缘必然落在 Grid 右边缘上，宽度差只会让左边的按钮整体平移；③ "`ShowMoreOptions` 初值永远是 false"（`Transcode == 0` 恰是字段默认值，setter 会因新旧值相同而不触发 `OnTypeChanged`，那样按钮与面板会整块消失）——不成立：`AddTaskView.Window_Loaded` 会 `SetAndNotify(nameof(Type), Type)`，而这个方法在"新旧值相等"的分支里用反射主动调用了 `OnTypeChanged`（`ViewModelBase.cs:109-110`），既有 `ShowTimeClip`/`CanChangeInputsCount` 也依赖同一条链。另外确认主题给 `ToggleButton` 的**选中态**本来就配了专用填充（`ToggleButtonBackgroundChecked`），展开时与相邻两个 `Button` 不同色属 Fluent 的设计意图，不是缺陷；未选中态是否像素一致仍未定论，由肉眼定。

【已知】① 加载环可能挂在"不是发起操作的那个窗口"上（`WindowBusyOverlay.FindForegroundWindow` 取当时的前台窗口；`ChildWindowManager` 是先 `initialize(view)` 后建窗），跨窗口重叠时更是"后发起的窗口完全无环"（见上面【说明】，彻底解法是按窗口计数）。② 非转码任务里勾「输入为图像帧序列」只会把输入帧率静默改成 30，而"图像序列路径归一化"（`FFmpegTaskService.cs:493-500` 的 `GetSequence`）只存在于转码执行器。③「更多」是 `ToggleButton`，不吃本面板给 `Button` 写的隐式样式（`FileIOPanel.xaml:26-31`），只手工补了 `Height="32"`，宽度/内边距可能与相邻两个按钮不一致，需肉眼确认。

## 20260918（裁剪的时间传不回输入行：子进程标准输出被"尝试保存配置"污染）

【WPF】现象：输入行点【裁剪】→ 在裁剪窗口里设好起止 → 点【完成】→ 输入行的开始/结束时间框仍然是空的（用户报"点了完成但没显示时间"）。日志（`Generation\bin\Debug\SimpleFFmpegGUI.WPF\logs\SimpleFFmpegGUI<日期>.log`）里当天只有几条"程序启动"、没有任何异常 → 子进程是正常起来的，只是结果没回到主进程。

【WPF】**根因**（先加诊断才看得出来）：子进程把结果写回主进程靠的是标准输出——`CutWindowViewModel.Apply` 里 `Console.Write("{0},{1}", From, To)`，主进程 `ClipAsync` 读 stdout、`Split(',')` 再 `TimeSpan.TryParse`。而子进程**退出时要保存配置**，Core 的 `ConfigService.SaveAsync` 第一行是一句遗留的调试输出 `Console.WriteLine("尝试保存配置")`；那句结果**没带换行**，于是两段粘成一行：

```
00:02:47.9360000,00:03:36.9600000尝试保存配置
```

第二个 `TimeSpan.TryParse` 因此失败、整条结果被丢掉——而旧代码在解析失败时**什么都不做**（不提示、不记日志、不写日志文件），表现就是"点了完成，时间没变"，从任何界面都看不出是哪一环断了。**教训**：跨进程用 stdout 传结果时，写入端必须"成行 + flush"，读取端必须容忍同一管道上的其他输出；再加上"失败要有声音"，这类问题本来不该花两轮才定位。

【WPF】修法（两端都收紧）：① `CutWindowViewModel.Apply` 改用 `Console.WriteLine` + `Console.Out.Flush()`，让结果自成一行。② `ClipAsync` 改为逐行找**第一个能解析成两个 TimeSpan 的行**（新增静态方法 `TryParseCutResult`），这样子进程 stdout 里混进别的行（无论出现在结果之前还是之后）都不影响解析；解析不到时记一条 Warn（退出码 + 原始输出）并弹"裁剪窗口没有返回时间，请重试"——把静默失败换成可诊断的失败。
③ 顺带修掉 `CutWindow.Window_Loaded` 里判断结束时间的复制粘贴笔误：`if (args[1] != "-")` 应为 `args[2]`。原来的后果：输入行里**只设了开始时间**（结束传 `-`）时，子进程会拿 `"-"` 去 `TimeSpan.TryParse` → 失败 → 弹"无法解析结束时间"并 `Shutdown()`，**一个字节都没写回来**；反过来 From 传 `-`、To 有值时会静默丢弃结束时间。

【已知】Core 的 `ConfigService` 拿 `Console.WriteLine` 当日志（`SaveAsync` 开头那句"尝试保存配置"、读 config 失败时的 `ex.Message` 等）。对 GUI 宿主、尤其是这次要把 stdout 当协议用的子进程来说就是污染源。本次**不动 Core**（靠读取端逐行解析兜住）；要清就与 Core 的日志改造一起做。

