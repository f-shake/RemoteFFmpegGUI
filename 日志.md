# FFmpeg工具箱日志

## 20210805

做了一番比较以后，最终确定如下结构，并做了初步测试：


|项目名|项目名（中文）|介绍|
|-|-|-|
|Core|核心|提供Host和WPF、Web的契约以及公共方法|
|Host|主机|对FFmpeg进行包装，实现其功能，并通过NamedPipe进行发布|
|WPF|桌面GUI|本地执行的桌面端的GUI实现|
|WebAPI|服务端|使用ASP.NetCore实现的服务器|
|Web|Web客户端|使用Vue.js实现的网页端|

完成了Host与WebAPI之间的连接、WebAPI和Web之间的连接

## 20210810

搭建中。。初步完成了新增转码任务

## 20210811

【Core】新增了数据库连接

【Core】初步完成了任务队列系统

【Web】新增任务列表界面

## 20210812

【Web】进一步完成了任务的删除等、完成新增编码任务的界面等

【Web】优化界面，初步完成编码预设的后端

## 20210813

【Core】【Web】新增多文件拼接的支持

【Core】【Web】新增视频裁剪的支持

【Web】支持了新增、更新、调用预设

【Web】【Web】支持了额外参数

## 20210814

【Core】将Host的众多类移到Core中，Host仅用于提供管道通信

【Core】将DbContext改为单例模式

【Web】支持自定义管道名

【Web】新增FTP服务器功能

## 20210815

【Web】修改管道客户端类为依赖注入

【Web】新增在调用管道时，若检测到提供商未运行，则会自动启动

【Web】完成对任务列表的分页和筛选

【Web】修复了获取数据库信息时，并行查询报错的BUG

【Web】新增预设列表和日志列表

【Web】优化了错误信息的提示

## 20210816

【Web】新增支持上传小文件和直接下载输出文件

【Web】优化：在滚动时，左侧导航栏将固定

【Web】修复了当IIS无权限访问输入输出文件夹时无法使用的问题，通过Host访问来解决

【Web】优化了UI

## 20210817

【Web】优化了UI

【Web】新增支持视频裁剪设置中，可以粘贴一串时间字符串后自动解析为时分秒

【Core】【Web】新增支持不导出视频或不导出音频

## 20210818

【Web】优化了UI

【Core】【Web】支持指定视频容器

【Core】【Web】新增合并音视频功能

【Web】基本完成token认证功能

## 20210819

【Core】【Web】新增视频对比功能

【Web】新增重制时，可以保留文件

【Core】【Web】新增WebM的VP9和OPUS编码支持

## 20210820

【Web】新增编辑预设功能

【Web】优化编码参数显示效果，原来是JSON，现在改为了格式化后的表单

【Web】优化了视频裁剪策略，支持单独设置开始时间、视频长度、结束时间

【Web】优化了视频裁剪设置的UI

## 20210821

【Web】新增支持Host设置开机自启，支持设置工作目录为当前目录

【Core】【Web】新增自定义参数任务

## 20210822

【Core】【Web】分离普通转码和视频拼接，普通转码输入多个文件时将输出多个文件。

【Core】【Web】转码的输入裁剪将和输入文件关联，而不是输出参数

## 20210823

【Web】改成了使用数据生成的菜单

【Web】做了主页

## 20210824

【WebAPP】新增由WinForms包装的可一键打开的桌面程序入口

## 20210825

【WebAPP】优化了WebApp：

- 退出时若在执行任务则禁止关闭
- 改为F12打开开发者选项
- 浏览器改为中文

【Web】新增支持从查看某一个任务的所有日志

## 20210826

【Core】【Web】接入了MediaInfo，查询媒体信息时可以查看更详细的信息

## 20210827

【Web】支持了选择子文件夹内的输入文件

【Core】将进程处理方法换成了自己的，更加好控制

## 20210830-0906

【WPF】基本完成了WPF版本

## 20210905

【Web】修复了启用裁剪的任务加入队列后，新的任务虽然仍然勾选裁剪时间，但实际设置无效的BUG

【Web】优化时间裁剪设置，解析文本框保持可用，解析后自动启动该项裁剪

【Web】设置输入文件后将不会自动应用输出文件，而是在输出文件为空时，认为输出相同名称的文件。

## 20210908

【WPF】新增从任务列表中查看选中任务的日志

【WPF】新增日志根据类型进行筛选

【WPF】新增拖放加入文件

## 20210909

【WPF】修复了状态面板在有任务执行时反而显示无任务的BUG

【WPF】修复了视频比较不可用的BUG

【WPF】做了一些优化

## 20210911

【WPF】新增文件拖入主窗口即可新增任务

【WPF】新增任务栏进度条

【WPF】修复了一些新增任务时等参数面板的BUG

【WPF】修复了在任务进行时新增任务会报错的BUG

【WPF】新增ffmpeg和ffprobe命令的检测

【WPF】新增支持打开输入文件和输入文件的目录

## 20210912

【WPF】优化时间裁剪操作，输入框将保持可用，并与选择框联动

## 20210913

【WPF】修复了编辑完参数后，编辑框未失去焦点导致没有更新源数据的BUG

【WPF】修改任务界面，仅显示处理中任务的进度条，其他任务显示时间

## 20210919-20

【WPF】新增媒体信息查询

## 20210920

【WPF】新增平滑滚动

## 20210927

【WPF】修复了拼接时部分场景下报错的BUG

【WPF】修复了不可修改文件数目的任务类型仍可新增任务的BUG

## 20210930-1001

【WPF】新增视频可视化裁剪

【WPF】修改新建任务面板不再为主窗口的子窗口

## 20211002

【WPF】视频可视化裁剪新增快进和快退按钮

【WPF】优化新增任务，解决了新增时的卡顿，新增任务的同时开始队列改为切换按钮，新增后将切换到主窗口

【WPF】修复了更新预设无效的BUG

【WPF】新增删除预设功能

【WPF】修改了运行时关闭窗口的逻辑，当有任务正在进行中时，点击关闭按钮将会最小化到托盘而不询问。并取消了最小化到托盘按钮

【WPF】新增设置界面，可以设置平滑滚动和远程主机

【WPF】新增添加任务到远程主机功能

## 20211003

标注了各条日志所属的项目

【Web】修复了新增拼接、合并等任务时输出不可为空、不会检测输入文件存在性等BUG

【WPF】优化了任务详情显示

【WPF】新增创建任务后清空文件、创建任务后关闭窗口选项

【WPF】视频裁剪新增使用Ctrl和Shift切换快进快退的长度等，新增帧率显示

## 20211004

【WPF】优化了任务列表的显示，仅显示进行中的、排队中的任务以及其它在本次程序开启后的任务（类似Adobe Media Encoder

【WPF】新增所有任务显示窗口，支持分页

## 20211005

【WPF】修复了新增所有任务窗口部分按钮不隐藏的BUG

## 20211007

【WPF】修复了任务列表下方按钮点击报错的BUG

## 20211121

【WPF】调整了主界面大小等

【WPF】修复了帧率参数缺失的BUG

【WPF】新增预设窗口，支持删除、导入、导出

## 20211205

【Web】修复了文件名过长时，文件选择框中的其他文件选择不全或不显示的BUG

## 20220109

升级到了.NET6

所有的生成文件设置为统一的目录

新增生成Web包的批处理

## 20220122

【Core】（应该）修复了暂停后显示的剩余时间会边长的BUG

## 20220123

更新了部署方式，支持一建生成发布和部署。

【WPF】修改了音视频输出选项，设置更方便

【Core】【WPF】新增支持手动设置视频或音频的编码器

【Core】【WPF】新增支持设置视频像素格式

【Core】【WPF】新增支持画面比例设置

【Core】【WPF】修改分辨率修改为文本型，可以设置除了基础的i*j之类的形式（Web版本暂时未改，导致无法使用）

【WPF】修改了一些UI细节

【WPF】新增查看输出参数功能

【Core】【WPF】新增支持设置每一类的默认预设，打开时将会自动加载该类型的默认预设

## 20220131

【Web】适配了后端，新增了自定义分辨率、画面比例和像素格式设置

## 20220205

【WPF】新增拖放文件到主窗口时，弹出选择对话框，可选择加入任务或查询信息

【WPF】修复了添加任务错误，但仍然会切到主窗口的BUG

## 20220208

【Web】新增导入导出预设的功能

【Web】修复了含有Token时无法上传文件的BUG

## 20220210

【WPF】修改为单窗口应用，左侧为固定的任务列表和进行中的任务信息区域，右侧为TabControl。

## 20220307

【WPF】修复了设置默认预设后不起作用的BUG

## 20220309

【WPF】新增记住上次任务参数的功能

## 20220310

【WPF】完成了默认输出目录的设置界面，但还未进行实现

## 20220311

【WPF】修复了打开新建任务页面时闪退的BUG

## 20220313

【WPF】修改新建任务时，分开设置输出目录和输出文件名

【WPF】完成了默认输出目录的逻辑

【Core】修复了进程结束后仍然有输出，导致无法捕捉到视频对比结果的BUG

## 20220319

【WPF】尝试使用多进程的方式规避ModernWpf库和FFME库冲突导致的视频裁剪界面崩溃的BUG

## 20220325

【WPF】优化了视频分割，使用单个程序集，并更改为模态窗口

【WPF】修复了切换Tab时会重复初始化的BUG

## 20220415

【WPF】修复了删除所有预设后界面被禁用的BUG

## 20220416

【WPF】修复了部分无法获取长度的视频或脚本（avs）处理时报错的BUG

## 20220424

【WPF】修复了应用的记忆或预设，视频或音频策略为“复制”时，若切换到“重新编码”后，参数设置无效且创建任务报错的BUG

【WPF】修复了建立副本时，输出目录错误的BUG

## 20220430

【WPF】修复了任务列表中的进度处于IsIndeterminate状态的BUG

## 20220502

【WPF】新增编码性能测试功能

## 20220503

【WPF】优化编码性能测试功能（去掉了勾选框、增加了子进度条、不导出音频等）

## 20220504

【WPF】修复了编码性能测试VP9报错的BUG

【WPF】修复了VP9无法跑满全部核心的BUG

【WPF】优化编码性能测试的得分算法，支持不全部测完也能获取得分

【WPF】新增记忆窗口最大化状态

【WPF】取消预设的“应用”按钮，改为点击预设卡片应用

【WPF】修复了浅色模式下Tab头显示不正确的BUG

## 20220710

【Web】新增远程关机、队列完成后自动关机功能

## 20220714

【Web】新增显示CPU占用率功能

## 20220826

【WPF】修复了拖放文件新建任务时，输入文件的数量可能小于允许的最小文件数量的BUG

## 20220902、06

【Core】【Web】新增队列计划开始功能

## 20221113

【WPF】修复和优化了一些界面问题

【Core】优化了运行错误的显示

## 20221126

【WPF】【Web】优化支持了WPF提交给远程的文件支持在输入目录中的子目录中

## 20221203

【Web】修复了远程提交的作业输出文件名错误导致执行失败的BUG

【WPF】修复了界面错位的BUG

## 20221210

【Web】修复了远程提交时，若输出文件名为空，会出错的BUG

## 20221217

【WPF】优化了预设管理界面，可以在预设管理处修改预设值、设置默认预设

## 202301040

【WPF】修复了拖放超过目标类型最大支持的文件数量的文件后报错的BUG

## 20230107

【Code】修复了部分FFmpeg输出无法识别导致无法显示进度的BUG

【Code】【WPF】【Web】新增支持H264和H265的二次编码

## 20230109

【Core】修复了视频拼接时无进度条的BUG

【Core】修复了目标文件名尾部带空格时出现错误的BUG

【Core】优化了错误信息的提取

【Core】优化了日志的数据库写入，加快了FFmpeg大量输出时的速度

## 20230123

【Web】修复和优化了一些Web界面问题

【Core】【WPF】新增AV1支持（未完全）

## 20230124

【Core】【WPF】优化了编码测试和AV1支持

## 20230125

【Core】优化了AV1的支持，新增SVT-AV1编码器的支持

【WPF】新增AV1支持

【WPF】优化编码测试

【Core】修复了视频比较不显示进度的BUG

【WPF】新增FFmpeg输出命令行

## 20230127

【Core】为增加灵活性，摆脱使用FFMpegCore模块的依赖，重构FFmpeg参数生成模块，手动生成FFmpeg参数。

## 20230128

【WPF】修复了编码测试码率不可修改的BUG

【Web】修复了显示超过1个小时的时长进度时，显示出错的BUG

## 20230202

【Core】优化给VP9和AV1自动加上支持多线程的参数

【Core】新增VMAF编码评价指标

【Core】修复了一些BUG（SVTAV1不支持最大码率、BufferSize设置有误、x264和x265的最大预设值错误、部分多文件任务进度异常等）

【WPF】编码测试新增VMAF，支持了配置记忆

## 20230203

【WPF】编码测试新增CPU占用率指标

【WPF】优化编码测试CPU占用率和FPS指标

## 20230207

【Core】新增明确SVTAV1不支持2Pass

【Core】优化了2Pass时的进度条显示，不会再分两次

【WPF】优化了勾选开关后，将自动赋默认值

## 20230208

【WPF】优化了修改视频编码后，将自动赋默认值

## 20230221

【WPF】编码测试新增CRF模式

【WPF】编码测试新增码率补偿功能

【WPF】编码测试新增能够显示和导出输出文件的大小

## 20230222

【WPF】编码测试支持了重复利用素材，编码时输出文件名将附带相关信息，不再覆盖原有输出。

## 20230224

【Core】【WPF】新增支持图像序列帧转视频的功能

【Core】【WPF】新增支持输入参数中的帧率和自定义参数

【Core】优化当输出文件名存在非法字符时，自动删除非法字符

## 20230228

【Web】新增支持图像序列帧转视频的功能，但未测试，因为还未实现自动生成图像序列通配文件名功能

## 20230302

【Core】【WPF】新增根据输入文件猜测是否存在图像序列，并自动重命名

## 20230303

【Core】【Web】新增自动重命名图像序列

【Host】新增连续输出FFmpeg进度信息时，将覆盖上一条进度信息（仅当输出内容为一行时）

## 20230317

【Core】【WPF】【Web】彻底删除Ts中转方式的视频拼接

【WPF】修复了几个没有显示正确标签的BUG

【WPF】优化在新增任务界面切换任务类型时，能够保留文件

【Core】【WPF】【Web】修复了拼接时，若指定了输出文件名但未指定后缀名时报错的BUG

## 20230322

【Core】修复了二次编码因没有指定Format导致Pass=1时失败的BUG

## 20230323

【Core】【WPF】修复了剩余时间大于1天时，显示不准确的问题

【WPF】新增显示已用时间

## 20230405

【Core】【WPF】【Web】新增支持二次压制时，两次Pass采用不同的速度预设

## 20230406

【Core】修复了H265编码二次压制没有起作用的BUG

【Core】【WPF】【Web】由于发现问题，回退前一天的更新内容

【WPF】修复了已用时间和剩余时间的小时部分被四舍五入的BUG

【WPF】对状态框进行了一些UI修改

【WPF】为需要终止任务的按钮添加了二次提醒，并将取消任务改为异步

## 20230407

【Core】【WPF】重构了获取媒体信息的方法，全部使用`MediaInfo.exe`进行。同步修改了WPF界面

【Web】修改了状态条样式

## 20230408

【Web】对媒体信息页面进行了同步修改

【Core】为数据库增加了一些索引并完成数据库迁移功能，大大加快了读取速度

## 20230409

【Core】【WPF】新增支持进程优先级设置

【Core】尽量避免日志保存失败导致的程序崩溃

【Web】为Host增加了文件日志和全局异常捕获

## 20230411

【WPF】优化界面布局，当没有显示任何标签页时，将修改任务面板和状态面板的位置以优化显示效果

【WPF】支持在转码时显示缩略图（测试）

## 20230414

【WPF】修复了启动时若存在待处理任务，会导致堆栈溢出的BUG

## 20230415

【WPF】修复了弹出顶级面板时，任务状态面板会位于其上方的BUG

## 20230416

【WPF】优化在转码时的缩略图显示

## 20230509

【Core】新增支持同步输出文件的修改文件为输入文件的修改时间（暂无设置UI）

## 20230515

【Core】【Web】网页端新增支持进程优先级设置

【WPF】修复了完整状态面板与底栏重合的BUG

## 20230524

【Core】【WPF】基本实现了读取H265视频主要编码设置并应用到当前转码设置的功能：通过将文件拖放到视频编码设置上方。

## 20230528

【Core】新增H264的读取视频主要编码设置并应用到当前转码设置的功能

## 20230623

【Core】【WPF】【Web】优化同步文件修改时间功能为任务级别参数

## 20230718~19

【Web】新增支持转码时的缩略图显示

## 20231211

【All】更新到.NET8，更新了所有Nuget

【WPF】修复了因FFME.Windows不支持最新版本的ffmpeg导致无法裁剪视频的BUG（https://github.com/unosquare/ffmediaelement/issues/642）

【WPF】去除了WindowsAPICodePack的依赖，改为.NET8自带的文件选取器

【WPF】由于zgabi/ffmediaelement貌似修复了死锁问题，因此将裁剪功能修改为单进程运行，加快了启动速度

## 20231215

【WPF】修改提交任务并开始队列由开关改为独立按钮

## 20240404

【Core】【WPF】新增处理后删除输入文件的功能

【Core】修复了最新版本ffmpeg因输出状态改变导致部分场景（如复制流）下无法获取进度的BUG

【WPF】由于发现裁剪闪退问题为解决，因此回退了[cc72816](https://github.com/autodotua/RemoteFFmpegGUI/commit/cc7281699db1d342262676efa8abeb4c92a19a72)

## 20240413

【Web】新增处理后删除输入文件的功能

## 20240504

【WPF】任务列表支持了多选后批量操作

## 20240505-21

重构，全局依赖注入，WPF完全MVVM，引入CommunityToolkit.MVVM。

## 20240526

【Web】修复了多选取消任务无效的BUG

修复了多个dbContext实例导致数据不同步的BUG并修改Logger为静态类

## 20240618

【WPF】修改了创建任务的按钮逻辑，支持了创建远程任务并启动队列

## 20240901

【Web】将Host的Windows Service方式与Console方式分离，更好地实现开机自启

## 20240903

【WPF】新增支持批量添加文件或添加文件夹内所有文件

## 20240910

【WebAPI】修复了Web端执行时任务状态不改变的BUG：DbContext的生命周期由Transient改回Scoped，并去掉了PipeClient自动拉起Host进程的逻辑

【文档】更新了软件架构图

## 20241006

【构建】删除了WebApp项目（WinForms包装的一键启动入口）

## 20241029-30（newUI分支并入，tag v20241030）

【WPF】UI库由ModernWpf换成iNKORE.UI.WPF.Modern，并新建iNKORE.Extension项目重写通用对话框与进度环遮罩，不再使用内化的ModernWpf.FzExtension.dll

【WPF】为输出参数增加了单位显示和Tooltip提示（说明文案集中在ArgumentDescriptions）

## 20241102

【构建】把FFME预览用的ffmpeg分离到bin/ffmpeg_FFME（6.1.x），转码仍用bin/ffmpeg，两边的版本可以独立升级

【Core】运行错误信息改用异常消息；【构建】build.ps1去掉了非单文件版WPF的发布

## 20241104（gpu分支）

【Core】新增N卡硬件编码器的基础结构：N_H264 / N_H265 / N_AV1（h264_nvenc / hevc_nvenc / av1_nvenc）

【Core】编码器拆成SoftwareVideoCodec（CRF）与HardwareVideoCodec（CQ）两支，VideoCodec增加CrfLabel，并加上NVENC的p1~p7预设

## 20250127（gpu分支，tag v20250127）

【Core】修复了硬件编码的CQ参数：CRF()改为抽象方法，硬件编码器输出-cq、软件编码器输出-crf，并加了取值范围校验

【WPF】CRF标签改为按编码器动态显示（N卡显示"CQ"）；修复了主页删除状态面板后的显示误判

## 20250830（gpu分支经PR #11并入master）

【WPF】【Core】新增第三种提交方式"创建并独立执行"：任务不进主队列排队，立即单独起一个执行器与主队列并行

## 20251227

【Core】大幅降低了缩略图的开销：截图格式bmp→jpg、只取关键帧（-skip_frame nokey）、scale用fast_bilinear、限制线程与muxing队列，输出尺寸做成可配置的SnapshotSize

【Core】修复了Host中FFMpegCore未指定ffprobe位置导致视频长度获取错误的BUG

【构建】所有项目升级到.NET 10，新增Directory.Packages.props集中管理包版本

## 20260102

【WPF】修复了升级NuGet后的一些BUG；【构建】修复build脚本

## 20260130

【依赖】更新NuGet（EF Core Sqlite 10.0.2、Furion 4.9.8.10）

## 20260201（v2分支的起点，tag v20260201）

【WebAPI】移除了Furion：删掉Startup.cs，改用WebApplication.CreateBuilder的原生minimal hosting，注入/异常/CORS/Swagger全部换成原生实现

【WebAPI】引用由libs里的FzStandardLib.dll换成NuGet的FzLib；【Core】删掉IPipeService，Host的FtpManager搬进Core

## 20260202

【整体】删除了Host / Host.Console / Host.WindowsService三个工程与NamedPipe：WebAPI进程内直连Core、自己执行ffmpeg

【WebAPI】日志改用Serilog，TokenFilter与异常处理合并进AppActionFilter；【构建】新增Directory.Build.props统一输出路径

## 20260203

【Core】更广泛的依赖注入：干掉了FFmpegManager与FFmpegProcess，拆成FFmpegProcessService与FFmpegTaskService，新增DependencyInjectionExtension（AddFFmpegServices）

## 20260204

【Core】干掉了TaskManager / PresetManager这层"管理器"抽象，拆成Service（业务）与Repository（数据访问），各Manager相应改名

## 20260215~19

【测试】新建SimpleFFmpegGUI.WebTest：用WebApplicationFactory在进程内真启动WebAPI、跑真ffmpeg做接口测试

【Core】数据库改造：DbContext改为构造函数注入，连接串移入appsettings.json；【WebAPI】IConfiguration改IOptions、控制器返回参数统一

## 20260220~27

【测试】补齐了队列计划、预设、媒体信息、缩略图等测试；【Core】配置由数据库改为config.json（ConfigService）

【Core】大幅修改了目录结构与类名：TaskInfo→TaskEntity、OutputArguments→OutputParameters、TaskType.Code/Combine/Compare→Transcode/Mux/QualityCheck、Custom由3改为99，Parameters实现INPC

## 20260612

【WebAPI】API路由统一：只保留GET/POST，批量操作统一为/Batch/{action}

【Web】前端由Vue 2 + Element UI + Vue CLI升级到Vue 3 + Element Plus + Vite，并新增暗色模式

## 20260613

【Web】页面UI大改（统一卡片式布局、移动端自适应、媒体信息改为可折叠JSON树），并批量修复了预设保存、加入队列、计划时间等BUG

【WebAPI】修复了上传、预设导入、关机开关、批量Cancel/Reset等接口问题

## 20260614

【Core】新增v1.1→v2.0数据库自动迁移（DatabaseMigrator）：检测到旧库后自动备份，再重命名列、转换JSON、修Custom枚举、清Configs表

【WPF】适配master_v2的Core重构：全面改名与改命名空间（编译通过可启动，未经测试）

## 20260615

【WPF】【Core】多项BUG修复：Repository改用IDbContextFactory、输出路径去重、远程建任务改两步API、修复日志丢数据与HttpClient泄漏

【依赖】FFME升级到beta版（ffmpeg_FFME要求FFmpeg 7.0+）

## `AI` 20260818-19（v2 代码审查与修复）

按 [审查报告](docs/code-review/2026-08-18-v2-review.md) 与 [修复计划](docs/plan/2026-08-18-v2-fix-plan.md) 完成批次 0~5：

- 【FzLib 双轨】WPF 内化老版 FzLib 源码（`SimpleFFmpegGUI.WPF/FzLib/`，命名空间改为项目自己的），其余项目改用 NuGet FzLib 3.4.0-rc.2；libs 删除 5 个 FzLib 相关 DLL；删除 OneDrive 路径机制。
- 【功能】修复 Mux 任务被误建为 QualityCheck；队列 cancelQueue 永久置位；默认进程优先级接线（ConfigService）；预设编辑 409；Bearer undefined；processedOptions 字段名；Mux 长度处理（-shortest）死控件；默认输出路径落 OutputDir；预设导入 Type=3→Custom(99)；MediaInfo 编码设置缺键 500；迁移保留 Configs 用户配置；SnapshotSize 恢复为配置项。
- 【WPF】手动启动托管服务（DbLoggerService/AppLifetimeService）；远程主机协议适配（Bearer 前缀、Parameter 字段名、相对路径）；任务管理器并发字典；SyncModifiedTime/取消 NRE 防御。
- 【安全】路径穿越与绝对路径访问收口（FilePathHelper）；出厂 Token 清空与目录改为相对示例；AppActionFilter 异常统一 500 不回显详情；批量接口/上传/导入空引用与参数校验；快照临时文件用后即删；生产环境 UseExceptionHandler + Serilog 接入 host 管线。
- 【测试】WebTest 基建修复（ffmpeg/测试视频自动复制、19→40 个用例全绿）；迁移测试真实构造 v1 库验证；固定 sleep 改轮询。
- 【文档】README/CLAUDE.md 全面更新至 v2 现状；构建脚本适配（删 Host 发布、加 Windows 服务脚本）；清理死包与死代码。

## `AI` 20260819（WPF 退出死锁修复）

【WPF】修复主窗口关闭后进程无法退出：`Application_Exit` 在 UI 线程同步 `StopAsync().GetAwaiter().GetResult()`，而 `DbLoggerService.ExecuteAsync`（BackgroundService）启动时捕获了 Dispatcher 同步上下文，其循环延续被 post 回 UI 线程队列、恰被同步阻塞的 UI 线程卡住 → 死锁。Core 侧所有 await 补 `ConfigureAwait(false)`（DbLoggerService/AppLifetimeService/ConfigService，WebAPI 无同步上下文不受影响）；WPF 退出停止托管服务改为 5 秒超时 `Wait` 兜底。

## `AI` 20260912（实时推送与轮询泄漏修复）

【Web】修复了前端定时器泄漏：任务页 3 秒轮询、状态栏 10 秒快照轮询、电源页 5 秒 CPU 轮询等全部登记句柄并在卸载时清理，跨组件共享状态（队列状态、主题、窗口宽度）迁移到 pinia

【Core】新增单例的任务/队列变更通知器 `ITaskChangeNotifier`，在任务增删改、队列起止、计划时间设置等 12 处触发

【WebAPI】新增 SignalR 实时推送（Hub 挂在 `/api/queue-hub`，只走 WebSocket）：事件驱动 + 每秒采样一次推送瘦身 DTO，另加 `GET api/Queue/State` 供降级轮询一次拿齐状态；快照接口加字节缓存，同一视频的同一时间点只跑一次 ffmpeg

【Web】新增 `utils/realtime.ts`：连上就只靠推送，重连序列用尽后永久降级为 HTTP 轮询（处理中 3 秒/空闲 15 秒），顶栏加四态连接圆点；状态带自增序号且只接受更大的序号，避免降级与推送并存时旧数据盖掉新数据；任务列表不再定时轮询，改由"任务清单变了"的推送事件驱动刷新

【复核】两轮复核收口三处时序问题：读状态与分配序号必须在同一把锁里（否则队列结束后界面永久停在"处理中"）、每秒采样补 try/catch（否则一次推送失败会把整个 Web 服务停掉）、事件推送改到线程池；快照缓存移入 `Core/Caching/` 并补单测

## `AI` 20260912（WPF 运行状态边框贴边）

【WPF】运行状态边框原来只写了 `Grid.ColumnSpan`、没有 `RowSpan`，实际只占 `grdMain` 的第 0 行（底部命令栏那行始终在框外）。补上 `Grid.RowSpan="99"` 让它覆盖内容区的所有行

【WPF】新增 `MainWindow.UpdateRunStatusBorderMargin()`：挂在 `grdMain.LayoutUpdated` 上，按内容区相对窗口可视树的实测偏移（`TranslatePoint`）反推 `Margin`，让四条边贴到窗口可见边缘。两个不能写死的量：内容原点在标题栏下的实测偏移 `(1, 37)`，以及最大化时库的 `MaximizedWindowFixer` 设的 `Window.Padding` 6.5 DIP（高对比度下不补偿）

【验证】200% DPI 下截屏逐像素量：改后还原态与最大化态四边均为 0px，改前右、下各内缩 2 DIP

## `AI` 20260916（WPF 缩略图点击放大预览）

【WPF】任务卡片上的快照缩略图（360×256）点击后弹出独立窗口：滚轮以光标为锚点缩放、左键拖动平移、双击在"适应窗口 / 1:1"之间切换、Esc 关闭，工具栏另有缩小 / 1:1 / 适应窗口 / 放大四个按钮与缩放百分比、图片像素尺寸。方案见 [docs/plans/2026-09-16-WPF缩略图点击放大预览.md](docs/plans/2026-09-16-WPF缩略图点击放大预览.md)

【WPF】新增通用的 `Panels/ImagePreviewPanel`（只依赖"一张位图 + 一个标题"，别处要看图可直接复用）+ `ImagePreviewPanelViewModel` + 纯数学 `ImagePreviewMath`（锚点缩放/适应/1:1/平移钳制，不引用任何控件）；宿主仍走 `ViewWindow`（不直接 `new Window()`，避免 iNKORE backdrop 递归）

【WPF】几个取舍：窗口打开即冻结当帧（`BitmapCacheOption.OnLoad` + `Freeze()`，与文件彻底解耦）；同一帧重复点击只激活已开窗口；初始缩放为"适应窗口但不超过 100%"，平移做钳制，因此不存在"图被拖丢"的状态；快照未生成时（`Snapshot.Source` 为 null）光标与提示仍是不可点的样子

【已知】WPF 的快照临时文件（`%TEMP%\SimpleFFmpegGUI\snapshot\`，每张一个新 GUID 文件名）仍然只增不删：删除时机与主界面正锁着文件的图冲突，另案处理

## `AI` 20260917（WPF 预览窗口：图片缩成左上角小图 / 工具栏按钮底色）

【WPF】预览窗口里的图缩成一小块贴在左上角，而百分比、像素尺寸、矩阵、`ActualWidth` 全都显示正确。根因是**布局裁剪**：`DesiredSize` 被父级槽位压小时，WPF 会给元素自己挂一层布局裁剪，1920×1080 的图放进 1098×597 的视口只画得出左上角那一块，**再**被矩阵按整图尺寸缩放一次。它与父级的 `ClipToBounds` 无关（实测把 `ClipToBounds` 关掉结果不变）

【WPF】修法：给 `Image` 套一层 `Canvas`（`Canvas` 用无限尺寸测量子元素，`DesiredSize` 恢复成图片像素尺寸，布局裁剪消失），视口之外的部分仍由外层 `Border` 的 `ClipToBounds` 裁掉，并给视口加 `MinHeight="120"` 兜住"按内容定尺寸"的宿主。**这不是通用规则**：缩略图那张 `Border` 正是靠同一层布局裁剪裁掉 `Stretch="UniformToFill"` 的外溢，别照搬

【WPF】工具栏四个按钮加 `Background`/`BorderBrush` 为 `Transparent`：iNKORE 的按钮样式在常态给一层底色，局部值只决定常态、悬停与按下仍由主题给反馈；写具名的 `Transparent` 而不是 `{x:Null}`——null 会让按钮空白处不参与命中测试

【复核】两轮复核确认修法成立，并订正了注释与文档里两处写错的机理（先写成"位图被压扁"，后写成"裁剪来自 `ClipToBounds`"，都被带标记的图逐像素量否掉了）。**结论对不等于解释对**，写错的"为什么"会让后人去改错地方。另据复核补了 `Viewport_LostMouseCapture` → `EndDrag()`（拖动中丢捕获时光标卡住），以及开窗失败时 `Close()` 掉窗口（只摘静态表并不解开对象图）

【验证】`dotnet build SimpleFFmpegGUI.sln` 0 错误；真实面板探针截图后逐像素量，内容包围盒与矩阵预测一致（改前是 605×334 贴左上角）；按钮底色由不透明变为透明

## `AI` 20260917（WPF 忙碌遮罩：覆盖面补全 + 文案）

【WPF】忙碌遮罩原来只有 `Grid.ColumnSpan="99"`、没写 `RowSpan`，只占 `grdMain` 的第 0 行：底部那条 8 DIP 间隔行与 `CommandBar` 从来没被盖住（忙碌时按钮还能点），它当时那点 `Margin="-8"` 往下溢的正是那条间隔行的高度。现在补 `Grid.RowSpan="99"` 并去掉 `Margin`（该值与任何实测几何量都对不上）；遮罩仍放在 `RunStatusBorder` 之前，忙碌时运行指示保持鲜亮

【WPF】`WindowBusyOverlay.SetBusy(bool)` 改为 `SetBusy(string message)`，各调用方在发"忙碌"时说明这次在做什么（加入队列 / 提交到远程主机 / 裁剪 / 读取媒体信息 / 终止队列 / 测试连接 / 取消任务），显示在卡片"请稍等"下面那行

## `AI` 20260917（输入文件条：裁剪按钮挪到时间行，更多按钮挪到文件行）

【WPF】`FileIOPanel` 里每个输入文件的按钮按"谁管谁挨着谁"对调：文件行 = 浏览文件 / 移除当前 / **更多**，时间行右侧 = **裁剪**（它是对旁边两个时间框的操作）；裁剪按钮自己的 `Visibility` 删掉——整行已经按 `ShowTimeClip` 折叠了

【WPF】顺带的行为变化：`ShowTimeClip` 只在转码任务为真，而"更多"原来住在那行里，于是拼接 / 混流 / 质量测试 / 自定义任务此前根本看不到"更多"按钮；挪到文件行后所有类型都能展开

## `AI` 20260918（忙碌遮罩的配对与计数、「更多」按钮的归属）

【WPF】复核-修复-再复核查出的四条：① `ClipAsync` 的两个提前 return 在发"忙碌"之前、`finally` 却无条件发"不忙碌"，"输入框空着就点裁剪"会收掉**别的窗口**正在转的环（改为 `busySent` 标志）；② 环只有一个而 `SetBusy`/`SetNotBusy` 是按操作配对的，两个操作重叠时先结束的会关掉还在跑的（改为计数，减到 0 才收）；③ 500ms 淡出动画的 `Completed` 可能把正在淡入的环折叠掉（加 `if (!showing)` 守卫）；④ "更多"面板里的三个选项在拼接任务里全部被丢弃（执行器自己造输入、只取文件路径），新增 `ShowMoreOptions` 只在转码 / 混流 / 质量测试显示

【WPF】`grdMore` 从卡片最后一行挪到文件行正下方（行定义改为 Auto/4/Auto/8/Auto/Auto/*），8 DIP 的间隔补在面板内部的末尾——收起时整块被 `ScaleY=0` 压成 0，不会在文件行与时间行之间变成双倍空档

【复核】几条被否掉的猜测记下来免得后人重查：同构 Grid 放进 `ScrollViewer` 时 `*` 与 `Auto` 等价（该结构下以无限高度测量，实测两种写法数字完全相同）；两个 Grid 的最后一格都是 `Auto`，按钮右边缘必然对齐；`ShowMoreOptions` 的初值不会因"新旧值相等"而不触发（`SetAndNotify` 会在相等分支里主动调用）

【已知】① 环可能挂在"不是发起操作的那个窗口"上（`FindForegroundWindow` 取的是当时的前台窗口），跨窗口重叠时后发起操作的窗口整段无环，彻底解法是按窗口计数；② 非转码任务里勾"输入为图像帧序列"只会把输入帧率静默改成 30；③"更多"是 `ToggleButton`，不吃本面板给 `Button` 写的隐式样式，宽度与内边距需肉眼确认

## `AI` 20260918（裁剪的时间传不回输入行：子进程标准输出被"尝试保存配置"污染）

【WPF】现象：输入行点【裁剪】→ 设好起止 → 点【完成】，时间框仍然是空的；应用日志里没有任何异常（子进程正常起来，只是结果没回到主进程）

【WPF】**根因**：结果靠子进程的标准输出回传（`Apply` 里 `Console.Write`），而子进程退出时保存配置还会往 stdout 再写一句遗留的调试输出（Core 的 `ConfigService.SaveAsync` 里那句"尝试保存配置"），结果**没带换行**，两段粘成 `00:02:47.9360000,00:03:36.9600000尝试保存配置`，解析失败又被旧代码静默丢弃。**教训**：跨进程用 stdout 传结果，写入端要"成行 + flush"，读取端要容忍同一管道上的其他输出，失败要有声音

【WPF】修法：`Apply` 改用 `Console.WriteLine` + `Flush()`；`ClipAsync` 改为逐行找第一行能解析成两个 `TimeSpan` 的行，解析不到时记 Warn 并提示"裁剪窗口没有返回时间，请重试"。顺带修掉 `CutWindow.Window_Loaded` 里判断结束时间的复制粘贴笔误（`args[1]` 应为 `args[2]`）——只设了开始时间时，子进程会拿 `"-"` 去解析、报错退出，一个字节都写不回来

【已知】Core 的 `ConfigService` 拿 `Console.WriteLine` 当日志（"尝试保存配置"、读配置失败时的异常消息等），对把 stdout 当协议用的子进程就是污染源。本次不动 Core（靠读取端逐行解析兜住），要清就与 Core 的日志改造一起做
